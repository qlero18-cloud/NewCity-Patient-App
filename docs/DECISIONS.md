# Registro de decisiones

Una línea por decisión, con la opción que se rechazó. Se agrega al final conforme avanza el proyecto.

| # | Fecha | Decisión | Se rechazó | Por qué |
|---|---|---|---|---|
| D01 | 2026-08-05 | Web app / PWA que abre por QR | App nativa en App Store y Play | El paciente viene una o dos veces; instalar es fricción que no se recupera |
| D02 | 2026-08-05 | QR personal por visita con token de 128 bits | QR general + apellido y fecha de nacimiento | Apellido y fecha son adivinables: protección débil para datos de un paciente |
| D03 | 2026-08-05 | El itinerario muestra el nombre del estudio | Solo logística sin nombre del estudio | Decisión del cliente por claridad. Sube el nivel de protección requerido; se compensa con D05, INV-3 e INV-6 |
| D04 | 2026-08-05 | QPASS renderizado como QR o Code128 en pantalla | Código alfanumérico dictado | Coordinación ya emite un pase escaneable; la lectora del acceso lo lee de la pantalla |
| D05 | 2026-08-05 | El enlace caduca 24 h después de la última cita — ~~vigente~~ **modificada por D16** | 7 días · fin del día de la visita · sin caducidad | Cubre la visita y el regreso sin dejar datos de salud expuestos indefinidamente. El criterio de las 24 h se mantiene; lo que cambia es desde cuándo se cuentan |
| D06 | 2026-08-05 | Ruteo paso a paso dentro del v1, con rutas pre-escritas | Solo indicaciones escritas sin pasos | El cliente lo pidió dentro del v1. Se implementa sin posicionamiento en vivo, que exigiría balizas |
| D07 | 2026-08-05 | Los datos los captura un panel de coordinadores | Integración con el sistema del hospital | No depender de que exista API ni de los tiempos de un proveedor externo |
| D08 | 2026-08-05 | Bilingüe ES/EN con detección por navegador | Un solo idioma | Buena parte de los pacientes cruza desde San Diego |
| D09 | 2026-08-05 | Fuera del v1: resultados, pagos y login | Incluirlos | Cada uno mete requisitos regulatorios o de negocio que hunden el v1 |
| D10 | 2026-08-05 | JavaScript plano (ESM) + `node --test`, sin framework | React / Vite | El prototipo se publica como página autocontenida con CSP estricta; un framework no aporta y estorba al empaquetar |
| D11 | 2026-08-05 | `now` se inyecta en toda función de dominio (INV-1) | Leer el reloj dentro de las funciones | Sin esto, las reglas de caducidad y "siguiente paso" no son comprobables de forma determinista |
| D12 | 2026-08-05 | Todas las horas se muestran en `America/Tijuana` y etiquetadas | Convertir a la zona del dispositivo | Un paciente en zona horaria distinta vería una hora que no corresponde a su cita y la perdería |
| D13 | 2026-08-05 | El núcleo de dominio se construye y se prueba antes que la UI | Construir pantallas primero | Instrucción explícita del cliente; además las reglas son donde vive el riesgo real |
| D14 | 2026-08-05 | Compass es laboratorio e imagenología, no gastronomía | La suposición previa | Confirmado por el cliente. Obliga a corregir el mapa de la presentación ya entregada |
| D15 | 2026-08-05 | **El QPASS no caduca por tiempo** (`validUntil: null`) | Ventana de validez por visita o por cita | El paciente lo usa muchas veces al día durante toda su estancia. Un pase que vence a media visita lo deja en la puerta. La revocación pasa a ser el único control; se recomienda revocar al cierre de la estancia |
| D16 | 2026-08-05 | El enlace vive hasta `max(última cita, checkout) + 24 h` | Solo `última cita + 24 h` (D05 original) | Consecuencia de D15: si el enlace muriera antes del checkout, el paciente perdería la pantalla donde vive su pase de acceso |
| D17 | 2026-08-05 | "Tienda de conveniencia" queda fuera de `locations.js` hasta que se resuelva — ~~vigente~~ **resuelta por D27** | Inventar un 8º `mapPointId`, o forzarla dentro de un punto ya existente | phase-03 la lista como confirmada pero phase-04 fija exactamente 7 `mapPointId` sin ninguno para ella — inconsistencia entre fases ya aprobadas, no algo que esta fase deba resolver por su cuenta |
| D18 | 2026-08-05 | La geometría del mapa (fase 04) se portó leyendo `Mapa_complejo_ES.png` (salida ya renderizada) | Esperar a `mapa.py` (no está en el proyecto) o inventar un layout sin ninguna base real | El script fuente no existe en este entorno; la imagen sí, y ya es la mejor aproximación disponible al "esquema ya existente" que pide la fase — su contenido desactualizado (Compass mal etiquetado) no se copia, solo la idea espacial |
| D19 | 2026-08-05 | `build.py` (fase 05) es un aplanador ESM propio, por regex, de ~150 líneas | Agregar esbuild/rollup/webpack como dependencia | D10 ya fijó "sin framework, autocontenido bajo CSP estricta"; instalar un bundler real rompe esa postura de cero dependencias por un problema que el propio código (imports simples, sin `export default`, sin alias) no necesita resolver de forma general |
| D20 | 2026-08-05 | El prototipo acepta `?now=<ISO>` para anclar la hora "actual" a la fecha de la fixture que se esté mostrando | Usar siempre `Date.now()` real | Las 5 fixtures están fechadas en 2026; con el reloj real cualquier fecha posterior a sus citas las mostraría todas como vencidas (INV-3), inutilizando el recorrido de demostración. Sigue habiendo un único punto del código con permiso de leer el reloj real (`src/ui/app.js`), documentado como tal |
| D21 | 2026-08-06 | `qr.js` se acota a versión 3 / nivel M / modo byte (hasta 42 bytes UTF-8) | Implementar las 40 versiones × 4 niveles del spec completo | PRD §15.7 no tiene confirmado el formato real del payload todavía — construir el generador general sin saber qué hace falta de verdad es trabajo que se puede tirar. Un payload real más largo obliga a ampliar esto explícitamente (lanza excepción clara), no lo trunca en silencio |
| D22 | 2026-08-06 | El QR y el Code128 de esta fase se verificaron contra librerías de referencia externas instaladas en este entorno (`qrcodegen` de nayuki y `python-barcode`, ambas MIT) más un decodificador real (`cv2.QRCodeDetector` de OpenCV) | Confiar solo en que mi propio decodificador leyera de vuelta lo que mi propio codificador escribió | Un redondeo encode→decode con el mismo código en los dos lados no distingue "está bien" de "los dos comparten el mismo malentendido" — y de hecho así fue: aparecieron y se corrigieron 4 bugs reales (zigzag de columnas, dos transposiciones fila/columna en la información de formato, y una convención de orden de bits invertida) que ese redondeo nunca habría detectado por sí solo |
| D23 | 2026-08-06 | El prototipo se publica en Netlify (importando el mismo repo de GitHub, sin cambiar código) | GitHub Pages, elegido primero | El primer (y segundo, tras un "Re-run" manual) deploy de GitHub Pages nunca completó: se quedó en "Queued"/"In progress" sin avanzar ni mostrar un error diagnosticable desde fuera de la cuenta del cliente (posible límite de minutos de Actions del plan gratis, no confirmable sin acceso a Settings → Billing). Netlify construye en su propia infraestructura, no depende de esa cola |
| D24 | 2026-08-06 | `qr-demo.png` codifica `https://nchpatient.netlify.app/demo`, una redirección 302 propia (`_redirects`, mismo origen) hacia el enlace real con `?p=` y `now=` | Codificar la URL larga (con token y `now`) directamente | La URL larga pesa ~85 bytes UTF-8, más que los 42 a los que D21 acotó a propósito el generador de QR de la fase 06. Ampliar ese generador para este caso de uso distinto (una URL general que lee la cámara de un teléfono, no el QPASS que lee una lectora física) reabriría justo el trabajo que D21 evitó a propósito. La redirección es del propio sitio publicado, no un acortador de terceros — no agrega una dependencia externa nueva |
| D25 | 2026-08-06 | "The Yard" se renombra a "The Park Restaurante" en todo el proyecto (`plaza.js`, `routes.js`, `locations.js`, `complexMap.js`, PRD y fases 02/03/04/05) | Conservar "The Yard", o quitarlo sin reemplazo | El cliente aportó `directorio-plaza-exterior.pdf` (señalética oficial de la plaza) en fase 07: no lista "The Yard" bajo Gastronomía, sino "The Park Restaurante" (junto con José Café y Boka, antes desconocidos para el prototipo). El cliente confirmó de forma explícita que es el mismo local renombrado, no un cierre — se corrige el nombre en vez de solo marcarlo unconfirmed, mismo criterio que D14 con Compass |
| D26 | 2026-08-06 | La geometría de `complexMap.js` (fase 04) se rehace con dos niveles reales — Nivel Calle y Nivel Plaza — leyendo `directorio-plaza-exterior.pdf` | Dejar el esquema de una sola fila (fase 04 original), o esperar a planos completos del interior de la Torre Médica antes de tocar esto | El cliente pidió mejorar la geometría ahora que hay una referencia real, aunque fase 07 ya casi cierra. El documento nuevo solo cubre street-level/plaza-level de la plaza exterior, no el interior de la Torre Médica piso por piso (Piso 27 no aparece ahí) — sigue etiquetado "esquema referencial, no a escala" (D18), ahora con mejor información de a qué nivel pertenece cada uno de los 7 puntos. El ancho del viewBox (420) no se tocó a propósito, solo el alto, para no invalidar la medición real de radio de toque de la fase 04 — se volvió a medir con getBoundingClientRect() tras el cambio y dio el mismo resultado (52.27px a 375px de viewport) |
| D27 | 2026-08-06 | Los ~27 locales y amenidades restantes de `directorio-plaza-exterior.pdf` se agregan como `PlazaVenue` con un campo `category` nuevo (los 5 rótulos del documento + "amenidad"), NO como `Location`/`mapPointId` nuevos en el mapa SVG | Agregar un `mapPointId` por local (27 círculos tocables nuevos en el esquema), o dejarlos fuera del prototipo | El cliente pidió agregar todo lo listado, incluyendo José Café y las amenidades (cajeros, etc.). 27 puntos tocables saturarían un esquema pensado para una pantalla de teléfono, y `src/ui/screens/plaza.js` nunca usó `mapPointId` para "cómo llegar" (a diferencia de la ficha del mapa) — ahí es solo una referencia de zona, así que cada local nuevo apunta al punto existente más cercano del documento en vez de inventar uno propio. De paso resuelve D17 (7-Eleven = la "tienda de conveniencia") y PRD §15.3 (amenidades) para todo salvo wifi y zonas pet-friendly, que el documento tampoco trae |
| D28 | 2026-08-06 | Bug real corregido: `src/ui/components/tabs.js` renombra `data-tab="${id}"` a `data-nav="${id}"` en los 5 botones de la barra inferior | Dejarlo como estaba (no era una decisión de diseño, era un descuido) | El cliente reportó en vivo "el menu inferior no funciona" y "el mapa no esta actualizado". Causa real: `src/ui/app.js` solo conecta clic a `[data-nav]` (`root.querySelectorAll('[data-nav]')`); `tabs.js` usaba un nombre de atributo distinto que nunca se conectó a ningún listener, desde que existe ese archivo — la barra inferior (Inicio/Itinerario/Mapa/Plaza/Ayuda) nunca navegó al tocarla, en ningún despliegue. Un botón sin listener igual recibe el foco visual del navegador al tocarlo, por lo que no se notaba en una captura de pantalla estática. Explica los dos síntomas reportados a la vez: si "Mapa" en esa barra no reacciona al tocarlo, el cliente nunca llegó a ver la geometría de dos niveles de D26, con o sin razón para dudar del contenido del mapa en sí. Yo mismo había diagnosticado antes, en esta misma sesión, el mismo síntoma (clics que no navegaban en el sitio publicado) como una rareza de mi herramienta de navegador automatizado — y lo rodeé escribiendo `location.hash` directo en vez de investigar la causa real; ese diagnóstico anterior estaba mal, esto era el bug real todo el tiempo. Cobertura agregada: `test/ui/tabs.test.js` (el proyecto no trae un DOM falso para `node:test` — ver la nota en `test/ui/plaza.test.js` — así que la prueba es una aserción de substring sobre el HTML de `renderTabBar`, no un clic simulado; habría atrapado este bug el día que se escribió `tabs.js`) |
| D29 | 2026-08-06 | `docs/phases/phase-09-coordinator-demo.md` creado como fase nueva, separada de la fase 08 (que no se toca ni se adelanta) — demo de clic del panel de coordinadores, mismo enfoque sin backend/DB/auth de las fases 01–07. Incluye `QPass.format: 'image'` (el cliente aclaró que el QPASS es una imagen subida por la coordinadora, no un `payload` corto para `qr.js`/`code128.js`) | Ampliar directamente la fase 08 con esto, o resolver ahí las 6 decisiones abiertas de hosting/DB/auth solo para desbloquear el flujo de QPASS | El cliente pidió avanzar el panel de coordinadores sin esperar hosting/DB/auth; como esta demo no persiste nada de verdad, no necesita ninguna de las 6 decisiones abiertas de fase 08, así que puede construirse ya. Redactado por un equipo de 7 agentes (3 en paralelo redactando alcance/QPASS/memo de mapa, 1 sintetizando, 2 revisando en paralelo, 1 finalizando) a pedido explícito del cliente ("crea con un equipo de agentes"). La revisión adversarial encontró y corrigió dos problemas reales antes de que este documento se aprobara: (1) el borrador contradecía su propia promesa de "nada sobrevive un refresh" — `attachPassScreen` habría guardado la imagen del QPASS en `localStorage` vía `savePassCache` sin filtrar; se resuelve excluyendo los pases `format: 'image'` de ese guardado; (2) el borrador no explicaba cómo la imagen subida en la pestaña de `coordinator.html` llegaría a la pantalla del paciente en `app.html` — son dos documentos HTML separados sin puente de almacenamiento; se resuelve con una ruta `#/pass-preview` que reutiliza `renderPassScreen`/`attachPassScreen` de `pass.js` dentro de la misma pestaña de la coordinadora, en vez de cruzar documentos. Nota aparte, no de contenido: uno de los agentes escribió el documento a disco por su cuenta con la versión sin revisar (antes de estos dos arreglos) — se detectó al leer el archivo antes de confiar en él y se sobrescribió con la versión final ya revisada. El memo de estilo de mapa (respuesta a "el mapa no esta actualizaado") se entregó aparte, sin decisión tomada: 3 opciones con costo/beneficio, pendiente de que el cliente elija |
| D30 | 2026-08-06 | El cliente eligió la opción (b) del memo de D29: `assets/plaza-map-background.jpg` (recorte a 700×676 del JPG de impresión que aportó, sin el panel de directorio ni los logos) reemplaza la fila abstracta de D26 como fondo real del mapa; los 7 círculos se reposicionaron a mano leyendo esa imagen a resolución completa | Opción (a) del memo (dejar la fila abstracta) u opción (c) (mapa ilustrado a la medida, bloqueada por falta de un asset de diseño real) | El cliente adjuntó el JPG y pidió explícitamente la superposición. `mp_floor27` sigue sin aparecer en el documento (es un piso interior de la Torre Médica, no una zona de planta baja) — se aproxima junto al bloque H5/Lobby, mismo criterio de aproximación que D26 ya estableció para este punto. `mp_level1` tampoco tiene una posición exacta: el documento muestra "The Park Restaurante" y "Farmer's Table" en dos lugares distintos de Nivel Plaza, pero el límite de 7 puntos (D26/D27) no cambia, así que se usa la posición del bloque más grande de los dos (The Park). Las etiquetas de nivel en código (D26) se quitan: la imagen ya trae "NIVEL CALLE/PLAZA \| STREET/PLAZA LEVEL" impreso de forma bilingüe permanente, así que repetirlas por encima era redundante. El radio de toque subió de 32 a 46 (viewBox más ancho, 420→700, para no recortar ni deformar la imagen real) — medido de nuevo con `getBoundingClientRect()` en el navegador, no asumido: un primer intento con radio 44 midió 43.12px (por debajo del mínimo de 44px), con 46 dio 45.08px |
| D31 | 2026-08-06 | El criterio de aceptación "prueba física con lectora real" de fase 06 se aclara: tiene que ser contra un QPASS **real** (la imagen que suba una coordinadora, D29), no el QR generado por `qr.js` a partir de un `payload` de fixture | Dar por parcialmente cumplido este criterio con la prueba de fixture, o dejarlo ambiguo tal como estaba redactado | El cliente probó `qr-demo.png` en su teléfono (fase 07: URL, ícono al agregar a inicio) pero explícitamente no intentó la prueba de lectora porque el QR de esa pantalla sale de un `payload` de fixture (`payload-q1`, `src/data/fixtures.js`) sin ningún acceso real del edificio detrás — probarlo no habría confirmado nada. Confirma D29 desde el otro lado: el QPASS real nunca fue un `payload` que este generador tuviera que codificar, así que este criterio de fase 06 queda bloqueado hasta que exista una imagen real de QPASS, no hasta que el cliente tenga tiempo de probar la de fixture — son bloqueos distintos, y confundirlos habría dado por completada una prueba que no prueba nada |
| D32 | 2026-08-06 | Incidente real durante el workflow de implementación de fase 09: el sub-agente `integration-router` (encargado de `coordinatorApp.js` y `coordinator.html`) se detuvo con `API Error: Response stalled mid-stream` — sí alcanzó a escribir `coordinatorApp.js` en disco antes de cortarse, pero nunca llegó a escribir `coordinator.html` ni a reportar resultado (`journal.jsonl` del propio workflow: 9 entradas `started`, solo 8 `result`). En vez de relanzar el workflow completo, se verificó a mano (timestamps de archivo + lectura directa) qué sí quedó escrito, y se dejó que las fases de Revisión/Fix del mismo workflow, que corrieron después, notaran y llenaran el hueco real (`coordinator.html` faltante) | Relanzar el workflow de fase 09 completo desde cero, o confiar en el `agents_error:1` reportado sin comprobar qué sí se había escrito de verdad | Relanzar todo habría repetido ~56 min y ~1.46M tokens de trabajo ya hecho por los otros 8 de 9 agentes, para un solo agente cuya llamada de escritura de archivo sí se completó antes de que solo su respuesta de *texto* se cortara — verificar directo evitó tanto relanzar de más como dar por perdido trabajo que sí existía. Nota sobre este mismo registro: el incidente ocurrió antes de un corte de contexto de esta sesión; esta entrada se reconstruyó después buscando en la transcripción completa (líneas ~6234, 6987, 7009–7023) y se corroboró de forma independiente contra el `journal.jsonl` del workflow todavía en disco, en vez de describirlo de memoria |
| D33 | 2026-08-06 | "Editar cita" se agrega como cuarta acción propia de itinerario (fase 09): `coordinatorStore.editAppointment(visitId, appointmentId, {serviceName, durationMin, locationId}, now)`, que nunca toca `startsAt` ni `status`, disparado por un botón "Editar" nuevo en cada tarjeta que reutiliza el mismo patrón de `window.prompt()` encadenado que ya usaba "Mover" (3 prompts: nombre del estudio, duración, ubicación, reusando las llaves i18n `serviceNameLabel`/`durationLabel`/`locationLabel` ya existentes). La llave `coordinator.itinerary.save` (sin ningún uso real) se elimina; se agrega `coordinator.itinerary.edit` como única cadena nueva de verdad | Un formulario de edición en línea por tarjeta (más "pulido" pero sin precedente de estado local de UI en este proyecto); fusionar "editar" dentro de `moveAppointment`; repurponer la llave `save` ya existente en vez de agregar `edit` | El doc de fase 09 nombra "editarla" como acción propia, distinta de "moverla" — el enum cerrado de `Appointment.status` (PRD §7) no tiene, ni necesita, un valor para "editado" (confirmado con prueba dedicada: editar una cita ya `moved` conserva su status intacto). El formulario en línea habría sido la primera vez que este proyecto necesita estado de UI local por tarjeta — patrón nuevo sin precedente, para una demo que su propio doc encuadra como "demo de clic, no el MVP"; extender `window.prompt()` (que revisiones previas nunca marcaron como problema de calidad para "Mover") fue la opción más simple y consistente. `save` nunca tuvo uso real (confirmado por grep antes de tocarlo) — dejarlo muerto junto a un `edit` nuevo habría sido inconsistente con el criterio ya aplicado en este mismo archivo a `movedBadge`/`cancelledBadge` |
| D34 | 2026-08-06 | Bug real corregido: `attachPassScreen` (`src/ui/screens/pass.js`) gana una bandera opt-in `ctx.ephemeral` (default `undefined`/falsa) que, cuando es `true`, salta por completo el guardado en `localStorage`. Solo la ruta `#/pass-preview` de `coordinatorApp.js` la pasa como `true`; la sesión real del paciente (`app.js`) nunca la toca, así que su comportamiento queda byte a byte igual que fase 06. Se agrega `test/ui/pass.test.js`, primera prueba automatizada de este archivo desde que existe | Dejar `#/pass-preview` sin esta bandera (su estado heredado del workflow de fase 09), o corregir el bug confiando de nuevo solo en revisión manual, sin prueba dedicada | Sin la bandera, abrir la vista previa del pase desde el panel de coordinadores escribía en el MISMO `localStorage` del navegador el payload real de una visita ajena (la que esté armando la coordinadora, no la sesión del paciente) — contradice de forma directa la promesa explícita del doc de fase 09 ("nunca... localStorage"). El bug ya había pasado una revisión manual sin que nadie lo notara; se detectó solo porque un agente ejecutó `#/pass-preview` de verdad en vez de solo leer el código. Por eso esta bandera booleana rompe, de forma explícita y documentada, la disciplina de "pass.js es solo revisión manual" que viene desde fase 06 — mismo criterio que ya justificó `test/ui/nav.test.js` como excepción a "solo se prueba HTML por substring": se prueba el cableado de una bandera booleana, no el resto de la pantalla (símbolo, wake lock, navegación entre pases), que se queda sin prueba automatizada, como estaba |
| D35 | 2026-08-06 | Bug real corregido: se agrega la regla CSS `.nc-badge--neutral` (clara y oscura) a `BADGE_CSS` (`src/ui/components/badge.js`), con el mismo color de texto que `--nc-ink` (`src/ui/theme.js`) y un fondo intermedio entre tokens de tema ya existentes | Inventar un gris nuevo sin relación con el resto de la paleta, o dejar el hueco sin corregir por no existir en este proyecto precedente de prueba de contenido CSS | `renderBadge(text, variant = 'neutral')` tiene "neutral" como default desde fase 05 y ya se llamaba así en dos pantallas reales — `hours.js` ("horario desconocido") y `screens/coordinator/qpass.js` ("QPASS pendiente", fase 09) — pero nunca existió la regla: esos dos badges se pintaban sin ningún estilo, solo el markup de `.nc-badge` desnudo. No era un hueco hipotético, sino un bug de renderizado ya en vivo en dos pantallas reales. A diferencia de los otros 3 colores (cada uno con un matiz semántico propio: aviso, actualizado, cancelado), "neutral" no necesita matiz propio — fundarlo en `--nc-ink`, el mismo color de texto ya usado en toda la app, evita introducir un color nuevo sin relación con la paleta ya probada |
| D36 | 2026-08-06 | Bug real corregido, reportado en vivo por el cliente ("solo veo visits, no veo lodging ni qpass"): se agrega `renderVisitSubnav(route, t)` en `coordinatorApp.js`, cableado en `render()` entre el encabezado y `<main>` cuando la ruta activa es itinerary/lodging/qpass — tres botones `data-nav` (D28) que llevan de cualquiera de las tres pantallas de una visita a las otras dos, reusando `.nc-button`/`.nc-button--primary` (THEME_CSS) para el estado activo y las mismas llaves i18n que cada pantalla ya usa en su propio `<h1>` (`coordinator.itinerary.title`/`.lodging.title`/`.qpass.title`), sin cadenas nuevas. Prueba nueva: `test/ui/coordinator/subnav.test.js` | Un formulario/menú de "ir a" aparte, o replicar el patrón `.nc-tab`/`.nc-tab--active` de `tabs.js` en vez de reusar `.nc-button`/`.nc-button--primary` | Ninguna de las cinco pantallas de `coordinator/` tenía un solo `[data-nav]` real hacia otra pantalla de la misma visita: `itinerary.js` y `lodging.js` no traían ninguno, y el único de `qpass.js` (`data-nav="pass-preview"`) solo aparece DESPUÉS de emitir un QPASS — así que tras elegir una visita, la única salida real era el botón "volver a visitas" del encabezado, aunque el propio doc de fase 09 describe un recorrido itinerario→qpass→hospedaje que nunca tuvo dónde hacer clic. Esto se me había pasado por completo en la verificación de fase 09 que ya había dado por "completa": crucé entre `#/itinerary`, `#/lodging` y `#/qpass` escribiendo `location.hash` directo por JavaScript en vez de dar clic de verdad, así que nunca ejercité el hueco — la distinción entre "se puede llegar por código" y "hay un elemento en pantalla en el que la coordinadora pueda dar clic" es justo la que se me pasó, y es la misma clase de error que ya documenta D28 (un bug real de cableado que una revisión manual sin clic real no atrapa). Esta vez la verificación sí fue con `computer{action:"left_click"}` sobre referencias reales del árbol de accesibilidad, en los dos idiomas, confirmando visualmente el estado activo en cada paso |
