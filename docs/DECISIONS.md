# Registro de decisiones

Una línea por decisión, con la opción que se rechazó. Se agrega al final conforme avanza el proyecto.

| # | Fecha | Decisión | Se rechazó | Por qué |
|---|---|---|---|---|
| D01 | 2026-08-05 | Web app / PWA que abre por QR | App nativa en App Store y Play | El paciente viene una o dos veces; instalar es fricción que no se recupera |
| D02 | 2026-08-05 | QR personal por visita con token de 128 bits | QR general + apellido y fecha de nacimiento | Apellido y fecha son adivinables: protección débil para datos de un paciente |
| D03 | 2026-08-05 | El itinerario muestra el nombre del estudio | Solo logística sin nombre del estudio | Decisión del cliente por claridad. Sube el nivel de protección requerido; se compensa con D05, INV-3 e INV-6 |
| D04 | 2026-08-05 | QPASS renderizado como QR o Code128 en pantalla | Código alfanumérico dictado | Coordinación ya emite un pase escaneable; la lectora del acceso lo lee de la pantalla |
| D05 | 2026-08-05 | El enlace caduca 24 h después de la última cita — ~~vigente~~ **modificada por D16** | 7 días · fin del día de la visita · sin caducidad | Cubre la visita y el regreso sin dejar datos de salud expuestos indefinidamente. El criterio de las 24 h se mantiene; lo que cambia es desde cuándo se cuentan |
| D06 | 2026-08-05 | Ruteo paso a paso dentro del v1, con rutas pre-escritas | Solo indicaciones escritas sin pasos | El cliente lo pidió dentro del v1. Se implementa sin posicionamiento en vivo, que exigiría balizas |
| D07 | 2026-08-05 | Los datos los captura un panel de coordinadores | Integración con el sistema del hospital | No depender de que exista API ni de los tiempos de un proveedor externo |
| D08 | 2026-08-05 | Bilingüe ES/EN con detección por navegador | Un solo idioma | Buena parte de los pacientes cruza desde San Diego |
| D09 | 2026-08-05 | Fuera del v1: resultados, pagos y login | Incluirlos | Cada uno mete requisitos regulatorios o de negocio que hunden el v1 |
| D10 | 2026-08-05 | JavaScript plano (ESM) + `node --test`, sin framework | React / Vite | El prototipo se publica como página autocontenida con CSP estricta; un framework no aporta y estorba al empaquetar |
| D11 | 2026-08-05 | `now` se inyecta en toda función de dominio (INV-1) | Leer el reloj dentro de las funciones | Sin esto, las reglas de caducidad y "siguiente paso" no son comprobables de forma determinista |
| D12 | 2026-08-05 | Todas las horas se muestran en `America/Tijuana` y etiquetadas | Convertir a la zona del dispositivo | Un paciente en zona horaria distinta vería una hora que no corresponde a su cita y la perdería |
| D13 | 2026-08-05 | El núcleo de dominio se construye y se prueba antes que la UI | Construir pantallas primero | Instrucción explícita del cliente; además las reglas son donde vive el riesgo real |
| D14 | 2026-08-05 | Compass es laboratorio e imagenología, no gastronomía | La suposición previa | Confirmado por el cliente. Obliga a corregir el mapa de la presentación ya entregada |
| D15 | 2026-08-05 | **El QPASS no caduca por tiempo** (`validUntil: null`) | Ventana de validez por visita o por cita | El paciente lo usa muchas veces al día durante toda su estancia. Un pase que vence a media visita lo deja en la puerta. La revocación pasa a ser el único control; se recomienda revocar al cierre de la estancia |
| D16 | 2026-08-05 | El enlace vive hasta `max(última cita, checkout) + 24 h` | Solo `última cita + 24 h` (D05 original) | Consecuencia de D15: si el enlace muriera antes del checkout, el paciente perdería la pantalla donde vive su pase de acceso |
| D17 | 2026-08-05 | "Tienda de conveniencia" queda fuera de `locations.js` hasta que se resuelva — ~~vigente~~ **resuelta por D27** | Inventar un 8º `mapPointId`, o forzarla dentro de un punto ya existente | phase-03 la lista como confirmada pero phase-04 fija exactamente 7 `mapPointId` sin ninguno para ella — inconsistencia entre fases ya aprobadas, no algo que esta fase deba resolver por su cuenta |
| D18 | 2026-08-05 | La geometría del mapa (fase 04) se portó leyendo `Mapa_complejo_ES.png` (salida ya renderizada) | Esperar a `mapa.py` (no está en el proyecto) o inventar un layout sin ninguna base real | El script fuente no existe en este entorno; la imagen sí, y ya es la mejor aproximación disponible al "esquema ya existente" que pide la fase — su contenido desactualizado (Compass mal etiquetado) no se copia, solo la idea espacial |
| D19 | 2026-08-05 | `build.py` (fase 05) es un aplanador ESM propio, por regex, de ~150 líneas | Agregar esbuild/rollup/webpack como dependencia | D10 ya fijó "sin framework, autocontenido bajo CSP estricta"; instalar un bundler real rompe esa postura de cero dependencias por un problema que el propio código (imports simples, sin `export default`, sin alias) no necesita resolver de forma general |
| D20 | 2026-08-05 | El prototipo acepta `?now=<ISO>` para anclar la hora "actual" a la fecha de la fixture que se esté mostrando | Usar siempre `Date.now()` real | Las 5 fixtures están fechadas en 2026; con el reloj real cualquier fecha posterior a sus citas las mostraría todas como vencidas (INV-3), inutilizando el recorrido de demostración. Sigue habiendo un único punto del código con permiso de leer el reloj real (`src/ui/app.js`), documentado como tal |
| D21 | 2026-08-06 | `qr.js` se acota a versión 3 / nivel M / modo byte (hasta 42 bytes UTF-8) | Implementar las 40 versiones × 4 niveles del spec completo | PRD §15.7 no tiene confirmado el formato real del payload todavía — construir el generador general sin saber qué hace falta de verdad es trabajo que se puede tirar. Un payload real más largo obliga a ampliar esto explícitamente (lanza excepción clara), no lo trunca en silencio |
| D22 | 2026-08-06 | El QR y el Code128 de esta fase se verificaron contra librerías de referencia externas instaladas en este entorno (`qrcodegen` de nayuki y `python-barcode`, ambas MIT) más un decodificador real (`cv2.QRCodeDetector` de OpenCV) | Confiar solo en que mi propio decodificador leyera de vuelta lo que mi propio codificador escribió | Un redondeo encode→decode con el mismo código en los dos lados no distingue "está bien" de "los dos comparten el mismo malentendido" — y de hecho así fue: aparecieron y se corrigieron 4 bugs reales (zigzag de columnas, dos transposiciones fila/columna en la información de formato, y una convención de orden de bits invertida) que ese redondeo nunca habría detectado por sí solo |
| D23 | 2026-08-06 | El prototipo se publica en Netlify (importando el mismo repo de GitHub, sin cambiar código) | GitHub Pages, elegido primero | El primer (y segundo, tras un "Re-run" manual) deploy de GitHub Pages nunca completó: se quedó en "Queued"/"In progress" sin avanzar ni mostrar un error diagnosticable desde fuera de la cuenta del cliente (posible límite de minutos de Actions del plan gratis, no confirmable sin acceso a Settings → Billing). Netlify construye en su propia infraestructura, no depende de esa cola |
| D24 | 2026-08-06 | `qr-demo.png` codifica `https://nchpatient.netlify.app/demo`, una redirección 302 propia (`_redirects`, mismo origen) hacia el enlace real con `?p=` y `now=` | Codificar la URL larga (con token y `now`) directamente | La URL larga pesa ~85 bytes UTF-8, más que los 42 a los que D21 acotó a propósito el generador de QR de la fase 06. Ampliar ese generador para este caso de uso distinto (una URL general que lee la cámara de un teléfono, no el QPASS que lee una lectora física) reabriría justo el trabajo que D21 evitó a propósito. La redirección es del propio sitio publicado, no un acortador de terceros — no agrega una dependencia externa nueva |
| D25 | 2026-08-06 | "The Yard" se renombra a "The Park Restaurante" en todo el proyecto (`plaza.js`, `routes.js`, `locations.js`, `complexMap.js`, PRD y fases 02/03/04/05) | Conservar "The Yard", o quitarlo sin reemplazo | El cliente aportó `directorio-plaza-exterior.pdf` (señalética oficial de la plaza) en fase 07: no lista "The Yard" bajo Gastronomía, sino "The Park Restaurante" (junto con José Café y Boka, antes desconocidos para el prototipo). El cliente confirmó de forma explícita que es el mismo local renombrado, no un cierre — se corrige el nombre en vez de solo marcarlo unconfirmed, mismo criterio que D14 con Compass |
| D26 | 2026-08-06 | La geometría de `complexMap.js` (fase 04) se rehace con dos niveles reales — Nivel Calle y Nivel Plaza — leyendo `directorio-plaza-exterior.pdf` | Dejar el esquema de una sola fila (fase 04 original), o esperar a planos completos del interior de la Torre Médica antes de tocar esto | El cliente pidió mejorar la geometría ahora que hay una referencia real, aunque fase 07 ya casi cierra. El documento nuevo solo cubre street-level/plaza-level de la plaza exterior, no el interior de la Torre Médica piso por piso (Piso 27 no aparece ahí) — sigue etiquetado "esquema referencial, no a escala" (D18), ahora con mejor información de a qué nivel pertenece cada uno de los 7 puntos. El ancho del viewBox (420) no se tocó a propósito, solo el alto, para no invalidar la medición real de radio de toque de la fase 04 — se volvió a medir con getBoundingClientRect() tras el cambio y dio el mismo resultado (52.27px a 375px de viewport) |
| D27 | 2026-08-06 | Los ~27 locales y amenidades restantes de `directorio-plaza-exterior.pdf` se agregan como `PlazaVenue` con un campo `category` nuevo (los 5 rótulos del documento + "amenidad"), NO como `Location`/`mapPointId` nuevos en el mapa SVG | Agregar un `mapPointId` por local (27 círculos tocables nuevos en el esquema), o dejarlos fuera del prototipo | El cliente pidió agregar todo lo listado, incluyendo José Café y las amenidades (cajeros, etc.). 27 puntos tocables saturarían un esquema pensado para una pantalla de teléfono, y `src/ui/screens/plaza.js` nunca usó `mapPointId` para "cómo llegar" (a diferencia de la ficha del mapa) — ahí es solo una referencia de zona, así que cada local nuevo apunta al punto existente más cercano del documento en vez de inventar uno propio. De paso resuelve D17 (7-Eleven = la "tienda de conveniencia") y PRD §15.3 (amenidades) para todo salvo wifi y zonas pet-friendly, que el documento tampoco trae |
| D28 | 2026-08-06 | Bug real corregido: `src/ui/components/tabs.js` renombra `data-tab="${id}"` a `data-nav="${id}"` en los 5 botones de la barra inferior | Dejarlo como estaba (no era una decisión de diseño, era un descuido) | El cliente reportó en vivo "el menu inferior no funciona" y "el mapa no esta actualizado". Causa real: `src/ui/app.js` solo conecta clic a `[data-nav]` (`root.querySelectorAll('[data-nav]')`); `tabs.js` usaba un nombre de atributo distinto que nunca se conectó a ningún listener, desde que existe ese archivo — la barra inferior (Inicio/Itinerario/Mapa/Plaza/Ayuda) nunca navegó al tocarla, en ningún despliegue. Un botón sin listener igual recibe el foco visual del navegador al tocarlo, por lo que no se notaba en una captura de pantalla estática. Explica los dos síntomas reportados a la vez: si "Mapa" en esa barra no reacciona al tocarlo, el cliente nunca llegó a ver la geometría de dos niveles de D26, con o sin razón para dudar del contenido del mapa en sí. Yo mismo había diagnosticado antes, en esta misma sesión, el mismo síntoma (clics que no navegaban en el sitio publicado) como una rareza de mi herramienta de navegador automatizado — y lo rodeé escribiendo `location.hash` directo en vez de investigar la causa real; ese diagnóstico anterior estaba mal, esto era el bug real todo el tiempo. Cobertura agregada: `test/ui/tabs.test.js` (el proyecto no trae un DOM falso para `node:test` — ver la nota en `test/ui/plaza.test.js` — así que la prueba es una aserción de substring sobre el HTML de `renderTabBar`, no un clic simulado; habría atrapado este bug el día que se escribió `tabs.js`) |
| D29 | 2026-08-06 | `docs/phases/phase-09-coordinator-demo.md` creado como fase nueva, separada de la fase 08 (que no se toca ni se adelanta) — demo de clic del panel de coordinadores, mismo enfoque sin backend/DB/auth de las fases 01–07. Incluye `QPass.format: 'image'` (el cliente aclaró que el QPASS es una imagen subida por la coordinadora, no un `payload` corto para `qr.js`/`code128.js`) | Ampliar directamente la fase 08 con esto, o resolver ahí las 6 decisiones abiertas de hosting/DB/auth solo para desbloquear el flujo de QPASS | El cliente pidió avanzar el panel de coordinadores sin esperar hosting/DB/auth; como esta demo no persiste nada de verdad, no necesita ninguna de las 6 decisiones abiertas de fase 08, así que puede construirse ya. Redactado por un equipo de 7 agentes (3 en paralelo redactando alcance/QPASS/memo de mapa, 1 sintetizando, 2 revisando en paralelo, 1 finalizando) a pedido explícito del cliente ("crea con un equipo de agentes"). La revisión adversarial encontró y corrigió dos problemas reales antes de que este documento se aprobara: (1) el borrador contradecía su propia promesa de "nada sobrevive un refresh" — `attachPassScreen` habría guardado la imagen del QPASS en `localStorage` vía `savePassCache` sin filtrar; se resuelve excluyendo los pases `format: 'image'` de ese guardado; (2) el borrador no explicaba cómo la imagen subida en la pestaña de `coordinator.html` llegaría a la pantalla del paciente en `app.html` — son dos documentos HTML separados sin puente de almacenamiento; se resuelve con una ruta `#/pass-preview` que reutiliza `renderPassScreen`/`attachPassScreen` de `pass.js` dentro de la misma pestaña de la coordinadora, en vez de cruzar documentos. Nota aparte, no de contenido: uno de los agentes escribió el documento a disco por su cuenta con la versión sin revisar (antes de estos dos arreglos) — se detectó al leer el archivo antes de confiar en él y se sobrescribió con la versión final ya revisada. El memo de estilo de mapa (respuesta a "el mapa no esta actualizaado") se entregó aparte, sin decisión tomada: 3 opciones con costo/beneficio, pendiente de que el cliente elija |
| D30 | 2026-08-06 | El cliente eligió la opción (b) del memo de D29: `assets/plaza-map-background.jpg` (recorte a 700×676 del JPG de impresión que aportó, sin el panel de directorio ni los logos) reemplaza la fila abstracta de D26 como fondo real del mapa; los 7 círculos se reposicionaron a mano leyendo esa imagen a resolución completa | Opción (a) del memo (dejar la fila abstracta) u opción (c) (mapa ilustrado a la medida, bloqueada por falta de un asset de diseño real) | El cliente adjuntó el JPG y pidió explícitamente la superposición. `mp_floor27` sigue sin aparecer en el documento (es un piso interior de la Torre Médica, no una zona de planta baja) — se aproxima junto al bloque H5/Lobby, mismo criterio de aproximación que D26 ya estableció para este punto. `mp_level1` tampoco tiene una posición exacta: el documento muestra "The Park Restaurante" y "Farmer's Table" en dos lugares distintos de Nivel Plaza, pero el límite de 7 puntos (D26/D27) no cambia, así que se usa la posición del bloque más grande de los dos (The Park). Las etiquetas de nivel en código (D26) se quitan: la imagen ya trae "NIVEL CALLE/PLAZA \| STREET/PLAZA LEVEL" impreso de forma bilingüe permanente, así que repetirlas por encima era redundante. El radio de toque subió de 32 a 46 (viewBox más ancho, 420→700, para no recortar ni deformar la imagen real) — medido de nuevo con `getBoundingClientRect()` en el navegador, no asumido: un primer intento con radio 44 midió 43.12px (por debajo del mínimo de 44px), con 46 dio 45.08px |
| D31 | 2026-08-06 | El criterio de aceptación "prueba física con lectora real" de fase 06 se aclara: tiene que ser contra un QPASS **real** (la imagen que suba una coordinadora, D29), no el QR generado por `qr.js` a partir de un `payload` de fixture | Dar por parcialmente cumplido este criterio con la prueba de fixture, o dejarlo ambiguo tal como estaba redactado | El cliente probó `qr-demo.png` en su teléfono (fase 07: URL, ícono al agregar a inicio) pero explícitamente no intentó la prueba de lectora porque el QR de esa pantalla sale de un `payload` de fixture (`payload-q1`, `src/data/fixtures.js`) sin ningún acceso real del edificio detrás — probarlo no habría confirmado nada. Confirma D29 desde el otro lado: el QPASS real nunca fue un `payload` que este generador tuviera que codificar, así que este criterio de fase 06 queda bloqueado hasta que exista una imagen real de QPASS, no hasta que el cliente tenga tiempo de probar la de fixture — son bloqueos distintos, y confundirlos habría dado por completada una prueba que no prueba nada |
| D32 | 2026-08-06 | Incidente real durante el workflow de implementación de fase 09: el sub-agente `integration-router` (encargado de `coordinatorApp.js` y `coordinator.html`) se detuvo con `API Error: Response stalled mid-stream` — sí alcanzó a escribir `coordinatorApp.js` en disco antes de cortarse, pero nunca llegó a escribir `coordinator.html` ni a reportar resultado (`journal.jsonl` del propio workflow: 9 entradas `started`, solo 8 `result`). En vez de relanzar el workflow completo, se verificó a mano (timestamps de archivo + lectura directa) qué sí quedó escrito, y se dejó que las fases de Revisión/Fix del mismo workflow, que corrieron después, notaran y llenaran el hueco real (`coordinator.html` faltante) | Relanzar el workflow de fase 09 completo desde cero, o confiar en el `agents_error:1` reportado sin comprobar qué sí se había escrito de verdad | Relanzar todo habría repetido ~56 min y ~1.46M tokens de trabajo ya hecho por los otros 8 de 9 agentes, para un solo agente cuya llamada de escritura de archivo sí se completó antes de que solo su respuesta de *texto* se cortara — verificar directo evitó tanto relanzar de más como dar por perdido trabajo que sí existía. Nota sobre este mismo registro: el incidente ocurrió antes de un corte de contexto de esta sesión; esta entrada se reconstruyó después buscando en la transcripción completa (líneas ~6234, 6987, 7009–7023) y se corroboró de forma independiente contra el `journal.jsonl` del workflow todavía en disco, en vez de describirlo de memoria |
| D33 | 2026-08-06 | "Editar cita" se agrega como cuarta acción propia de itinerario (fase 09): `coordinatorStore.editAppointment(visitId, appointmentId, {serviceName, durationMin, locationId}, now)`, que nunca toca `startsAt` ni `status`, disparado por un botón "Editar" nuevo en cada tarjeta que reutiliza el mismo patrón de `window.prompt()` encadenado que ya usaba "Mover" (3 prompts: nombre del estudio, duración, ubicación, reusando las llaves i18n `serviceNameLabel`/`durationLabel`/`locationLabel` ya existentes). La llave `coordinator.itinerary.save` (sin ningún uso real) se elimina; se agrega `coordinator.itinerary.edit` como única cadena nueva de verdad | Un formulario de edición en línea por tarjeta (más "pulido" pero sin precedente de estado local de UI en este proyecto); fusionar "editar" dentro de `moveAppointment`; repurponer la llave `save` ya existente en vez de agregar `edit` | El doc de fase 09 nombra "editarla" como acción propia, distinta de "moverla" — el enum cerrado de `Appointment.status` (PRD §7) no tiene, ni necesita, un valor para "editado" (confirmado con prueba dedicada: editar una cita ya `moved` conserva su status intacto). El formulario en línea habría sido la primera vez que este proyecto necesita estado de UI local por tarjeta — patrón nuevo sin precedente, para una demo que su propio doc encuadra como "demo de clic, no el MVP"; extender `window.prompt()` (que revisiones previas nunca marcaron como problema de calidad para "Mover") fue la opción más simple y consistente. `save` nunca tuvo uso real (confirmado por grep antes de tocarlo) — dejarlo muerto junto a un `edit` nuevo habría sido inconsistente con el criterio ya aplicado en este mismo archivo a `movedBadge`/`cancelledBadge` |
| D34 | 2026-08-06 | Bug real corregido: `attachPassScreen` (`src/ui/screens/pass.js`) gana una bandera opt-in `ctx.ephemeral` (default `undefined`/falsa) que, cuando es `true`, salta por completo el guardado en `localStorage`. Solo la ruta `#/pass-preview` de `coordinatorApp.js` la pasa como `true`; la sesión real del paciente (`app.js`) nunca la toca, así que su comportamiento queda byte a byte igual que fase 06. Se agrega `test/ui/pass.test.js`, primera prueba automatizada de este archivo desde que existe | Dejar `#/pass-preview` sin esta bandera (su estado heredado del workflow de fase 09), o corregir el bug confiando de nuevo solo en revisión manual, sin prueba dedicada | Sin la bandera, abrir la vista previa del pase desde el panel de coordinadores escribía en el MISMO `localStorage` del navegador el payload real de una visita ajena (la que esté armando la coordinadora, no la sesión del paciente) — contradice de forma directa la promesa explícita del doc de fase 09 ("nunca... localStorage"). El bug ya había pasado una revisión manual sin que nadie lo notara; se detectó solo porque un agente ejecutó `#/pass-preview` de verdad en vez de solo leer el código. Por eso esta bandera booleana rompe, de forma explícita y documentada, la disciplina de "pass.js es solo revisión manual" que viene desde fase 06 — mismo criterio que ya justificó `test/ui/nav.test.js` como excepción a "solo se prueba HTML por substring": se prueba el cableado de una bandera booleana, no el resto de la pantalla (símbolo, wake lock, navegación entre pases), que se queda sin prueba automatizada, como estaba |
| D35 | 2026-08-06 | Bug real corregido: se agrega la regla CSS `.nc-badge--neutral` (clara y oscura) a `BADGE_CSS` (`src/ui/components/badge.js`), con el mismo color de texto que `--nc-ink` (`src/ui/theme.js`) y un fondo intermedio entre tokens de tema ya existentes | Inventar un gris nuevo sin relación con el resto de la paleta, o dejar el hueco sin corregir por no existir en este proyecto precedente de prueba de contenido CSS | `renderBadge(text, variant = 'neutral')` tiene "neutral" como default desde fase 05 y ya se llamaba así en dos pantallas reales — `hours.js` ("horario desconocido") y `screens/coordinator/qpass.js` ("QPASS pendiente", fase 09) — pero nunca existió la regla: esos dos badges se pintaban sin ningún estilo, solo el markup de `.nc-badge` desnudo. No era un hueco hipotético, sino un bug de renderizado ya en vivo en dos pantallas reales. A diferencia de los otros 3 colores (cada uno con un matiz semántico propio: aviso, actualizado, cancelado), "neutral" no necesita matiz propio — fundarlo en `--nc-ink`, el mismo color de texto ya usado en toda la app, evita introducir un color nuevo sin relación con la paleta ya probada |
| D36 | 2026-08-06 | Bug real corregido, reportado en vivo por el cliente ("solo veo visits, no veo lodging ni qpass"): se agrega `renderVisitSubnav(route, t)` en `coordinatorApp.js`, cableado en `render()` entre el encabezado y `<main>` cuando la ruta activa es itinerary/lodging/qpass — tres botones `data-nav` (D28) que llevan de cualquiera de las tres pantallas de una visita a las otras dos, reusando `.nc-button`/`.nc-button--primary` (THEME_CSS) para el estado activo y las mismas llaves i18n que cada pantalla ya usa en su propio `<h1>` (`coordinator.itinerary.title`/`.lodging.title`/`.qpass.title`), sin cadenas nuevas. Prueba nueva: `test/ui/coordinator/subnav.test.js` | Un formulario/menú de "ir a" aparte, o replicar el patrón `.nc-tab`/`.nc-tab--active` de `tabs.js` en vez de reusar `.nc-button`/`.nc-button--primary` | Ninguna de las cinco pantallas de `coordinator/` tenía un solo `[data-nav]` real hacia otra pantalla de la misma visita: `itinerary.js` y `lodging.js` no traían ninguno, y el único de `qpass.js` (`data-nav="pass-preview"`) solo aparece DESPUÉS de emitir un QPASS — así que tras elegir una visita, la única salida real era el botón "volver a visitas" del encabezado, aunque el propio doc de fase 09 describe un recorrido itinerario→qpass→hospedaje que nunca tuvo dónde hacer clic. Esto se me había pasado por completo en la verificación de fase 09 que ya había dado por "completa": crucé entre `#/itinerary`, `#/lodging` y `#/qpass` escribiendo `location.hash` directo por JavaScript en vez de dar clic de verdad, así que nunca ejercité el hueco — la distinción entre "se puede llegar por código" y "hay un elemento en pantalla en el que la coordinadora pueda dar clic" es justo la que se me pasó, y es la misma clase de error que ya documenta D28 (un bug real de cableado que una revisión manual sin clic real no atrapa). Esta vez la verificación sí fue con `computer{action:"left_click"}` sobre referencias reales del árbol de accesibilidad, en los dos idiomas, confirmando visualmente el estado activo en cada paso |
| D40 | 2026-08-06 | Etapa A — Mover y Editar dejan de ser cadenas de `window.prompt()` (D33) y pasan a formularios inline dentro de cada tarjeta, con el mismo `<select>` de ubicación poblado desde `locations.js` que estrena el alta de cita, y `<input type="number" min>` para la duración. Los dos formularios se pintan siempre, ocultos con `[hidden]`, y el botón correspondiente los muestra | Conservar `window.prompt()` (la postura de D33); o poner el `<select>` solo en el alta y dejar Mover/Editar con texto libre | D33 justificó los prompts con "es una demo de clic, no el MVP" — premisa que el cliente retiró de forma explícita ("why are we talking about demo, i want a workgin solution"), así que su razón de ser se cayó. Y sin esto el dropdown se saltaba por la puerta de atrás: un `prompt()` no puede ofrecer opciones, así que bastaba teclear mal la ubicación al EDITAR una cita para romper justo el mapa que el `<select>` del alta acababa de asegurar. La objeción concreta de D33 ("sería la primera vez que este proyecto necesita estado de UI local por tarjeta") resultó no aplicar: pintando los dos formularios ocultos de entrada no hay ningún estado de "qué tarjeta está en edición" que mantener entre repintados — tras una mutación exitosa el router repinta desde cero y vuelven a quedar ocultos solos |
| D41 | 2026-08-06 | Etapa A — `resolveRoute` compone origen→`lobby_torre`→destino cuando no existe ruta directa (`HUB_LOCATION_ID`, `composed: true`), y el catálogo directo sube de 10 a 17 rutas para garantizar los 12 pares contra el lobby. La cobertura pasa de 10/42 a 42/42 pares ordenados (17 directos + 25 compuestos), y el test que fijaba "exactamente estos 10 pares" deja de ser exhaustivo a propósito | Redactar a mano los 32 pares faltantes; o dejar el hueco y que la pantalla de ruta apareciera vacía | Desde el estacionamiento —el origen por defecto (R7)— no había ruta a `piso27`, que es el destino más probable de toda la app: el paciente que llega en coche a su consulta veía una pantalla de ruta vacía, el caso más común del producto. Escribir 32 pares a mano habría sido inventar contenido par por par cuando TODOS siguen `unconfirmed: true` hasta que lleguen los planos oficiales (PRD §15.1) — más texto por confirmar, no más certeza. Componer por el lobby es igual de provisional pero es cierto: así se camina el edificio de verdad (se entra por ahí desde estacionamiento y hotel, y de ahí salen los elevadores a la torre y los accesos a Nivel 1 y farmacia). La ruta compuesta tiene exactamente la misma forma que una directa, así que el mapa la consume sin saber la diferencia |
| D42 | 2026-08-06 | Etapa A — Bug real corregido: la confirmación de guardado del formulario de hospedaje viaja como `ctx.flash` (una variable `pendingFlash` en el router que `render()` consume y limpia), no como un nodo que el handler inserta en el DOM. `clearFeedback()` además la borra al empezar cada intento | Insertar el mensaje en el DOM desde `attachLodgingScreen`, que es lo que hacía la primera versión | Dos bugs del mismo par, los dos encontrados en el navegador y ninguno atrapable por la suite. (1) `attachLodgingScreen` escribía "Hospedaje guardado." y acto seguido llamaba `onChange`, que ES `render`: el repintado borraba el mensaje antes de que nadie alcanzara a leerlo, así que guardar no daba ninguna señal — y el formulario es idéntico para alta y edición, así que tampoco cambiaba nada más en pantalla. (2) El camino inválido a propósito NO repinta (repintar borraría lo que la persona acaba de teclear, que es justo lo que hay que corregir), así que la confirmación del guardado ANTERIOR se quedaba junto al error nuevo — "Hospedaje guardado." encima de "Fecha no reconocida", peor que no decir nada. Ningún test automatizado podía ver ninguno de los dos: este proyecto no simula DOM en `node:test`, así que `attach*Screen` nunca se ejecuta en la suite (misma restricción que ya obligó a D34 a probar solo una bandera booleana). La lección que queda en el código es la forma, no el parche: una confirmación es estado de render, no un empujón imperativo al DOM |
| D43 | 2026-08-06 | Etapa A — El hallazgo #22 de la revisión ("`.nc-coord-*` está fuera de convención") se registra AL REVÉS de como se levantó, con evidencia: `.nc-coord-*` es la convención correcta, y la violación real era `coordinator/itinerary.js` usando `.nc-itin-what`, que pertenece al itinerario del PACIENTE. Se renombra a `.nc-coord-itin-what` y se cierra con `test/ui/coordinator/cssNamespace.test.js`, un guard general | Aplicar #22 tal como venía redactado (quitar los prefijos `.nc-coord-*`); o hacer el renombrado puntual sin guard | Un barrido de selectores por todo el bundle del coordinador no encontró un solo conflicto real: solo copias idénticas y deliberadas de `.nc-screen-title`/`.nc-empty-state`, que es exactamente lo que el encabezado de `itinerary.js` ya documenta como consecuencia del orden de carga. Aplicar el hallazgo como venía habría borrado la única convención que sí protege algo. El préstamo de `.nc-itin-what` hoy no rompe nada (son dos documentos distintos con reglas idénticas), pero es un conflicto latente de los caros: cambiar el tachado del paciente dejaría al coordinador con el estilo viejo en silencio, y si algún día los dos CSS caen en el mismo documento el orden de carga decide cuál gana. Segundo hallazgo invertido de esta misma etapa (#19 pedía `role="tablist"`, que ya estaba desde D36; lo que de verdad faltaba era `role="tabpanel"` en el `<main>`) — por eso se cierra con un test que lo verifica por construcción en cada corrida, en vez de con un renombrado que la siguiente pantalla puede volver a violar |
| D44 | 2026-08-06 | Etapa A — `revokeQpass(visitId, passId, now)` marca `revokedAt` y NUNCA borra el pase del arreglo; re-emitir crea un `id` nuevo (`q_2`) en vez de reusar el anterior. `isRevoked` pasa de local a exportado en `src/domain/passes.js` | Borrar el pase del arreglo al revocar; reusar el `id` al re-emitir; o duplicar la comparación de "revocado" dentro de la pantalla del coordinador | D15 dejó la revocación como el ÚNICO control del QPASS (no caduca por tiempo), así que tiene que quedar constancia de qué pase se revocó y cuándo — borrarlo destruye la única evidencia de que existió, justo en el mecanismo del que depende toda la seguridad del pase. Reusar el `id` haría indistinguible "se re-emitió" de "nunca se revocó". `isRevoked` se exporta en vez de reescribir la comparación en la pantalla porque INV-4 exige una sola definición de "revocado": lo que el coordinador ve como revocado y lo que `visiblePasses` le oculta al paciente tienen que coincidir por construcción, no por coincidencia. Verificado en el navegador de punta a punta (PDF rechazado por tipo, imagen de 3 MB rechazada por tamaño, PNG aceptado, emitir `q_1`, revocar, re-emitir `q_2`): `#/pass-preview` termina mostrando exactamente una imagen, la nueva |

| D37 | 2026-08-06 | Etapa B — El proyecto deja de ser estático: se agrega backend con Netlify Functions v2 + Netlify Blobs, y `@netlify/blobs` como primera dependencia de producción (se acabó el "cero dependencias"). `netlify.toml` versiona lo que hasta hoy solo existía en el panel de Netlify | Seguir sin backend con el panel en memoria (la premisa de la fase 09); una base de datos aparte (Supabase, Firebase); Netlify KV/Edge Config | Sin persistencia compartida no puede existir lo que el cliente preguntó: la coordinadora crea una visita, pero el token que acuña `coordinatorStore.createVisit` no puede resolverse desde el teléfono del paciente, así que el QR no tiene qué entregar. Blobs va con el hosting que ya se usa (D23) y no agrega un proveedor, ni una cuenta, ni una factura más para un hospital que todavía no decide su tratamiento de datos de salud. Esto revierte de forma explícita el "fuera del prototipo" de la fase 08 y la premisa de demo en memoria de la fase 09: el cliente retiró esa premisa ("i want a workgin solution") |
| D38 | 2026-08-06 | Etapa C — Autenticación propia dentro de las Functions: cuentas en un almacén Blobs aparte (`accounts`, no un prefijo del de visitas), contraseña con PBKDF2-SHA256 y sesión en cookie firmada con HMAC. Cero dependencias nuevas: todo sale de `node:crypto` | Netlify Identity (en modo mantenimiento desde 2024 y sin altas nuevas); Auth0 o Clerk; Supabase Auth; una contraseña compartida por todo el equipo | Elegiste cuentas individuales sobre contraseña compartida justamente para tener rastro de quién hizo cada cambio, y eso descarta la contraseña única de entrada. Entre los proveedores y hacerlo aquí, lo que decide no es la dificultad —integrar Auth0 es más fácil que esto— sino dónde acaban los datos: meter un proveedor externo agrega un tercero que ve quién entra a un sistema de expedientes clínicos, y esa es una decisión de tratamiento de datos del hospital (LFPDPPP), no una que deba tomar la implementación. Auth propia mantiene el dato dentro del mismo sitio que ya guarda las visitas y no suma superficie contractual. El costo es real y hay que decirlo: el proyecto se hace responsable de su propia criptografía de contraseñas y sesiones, que es exactamente lo que un proveedor te vende resuelto. Se acota usando primitivas estándar sin inventar nada (PBKDF2 y HMAC de `node:crypto`), y cubriendo cada decisión con pruebas — 100 casos nuevos en `test/server/` |
| D39 | 2026-08-06 | Etapa B — Tokens de visita de 128 bits (16 bytes) en base64url de 22 caracteres, vía `globalThis.crypto.getRandomValues`, en `src/domain/tokens.js`. Se acabaron `demo-token-v_1` y `fixture-token-v-demo1` como formato válido: `isValidToken` los rechaza. El validador NO exige que los 2 bits sobrantes del carácter 22 sean cero | `Math.random()`; UUID v4 (36 chars); `node:crypto.randomBytes` a secas; validar también los bits sobrantes | PRD §6.1 pide 128 bits y el prototipo tenía identificadores adivinables de un dígito. `getRandomValues` en vez de `node:crypto` porque el mismo módulo tiene que correr en la Function y en el navegador sin condicionales. base64url en vez de UUID porque el token va dentro del QR y cada carácter cuenta (D21: el generador de la fase 06 topa en 42 bytes). Exigir los bits sobrantes ataría el validador a exactamente 16 bytes para siempre: subir a 24 obligaría a cambiar dos lugares en vez de uno. Nota de numeración: el plan reservó D39 para "tokens de 128 bits **y** QR versión 4"; lo del QR es una decisión de la Etapa E que todavía no se toma, y llevará su propio número cuando se tome |
| D45 | 2026-08-06 | Etapa B — Toda la lógica de servidor vive en `src/server/*.js` sin un solo import de plataforma, contra un almacén llave/valor inyectado de cuatro métodos; `netlify/functions/*.mjs` son envoltorios de cinco líneas y `netlify/functions/_kv.mjs` es el único archivo del repo que sabe que existe Netlify Blobs. La suite corre con un `Map` en memoria que clona en cada `get`/`set` | Escribir los handlers directo en `netlify/functions/` y probarlos levantando `netlify dev`; usar un fake que devuelva referencias vivas | Un test que necesita `netlify dev` deja de correrse. La suite entera de la Etapa B (58 casos nuevos) corre con `npm test` y sin red, y probar el handler es de verdad —construyendo un `Request` y leyendo el `Response`— porque Functions v2 usa los estándar, globales en Node ≥18. El fake clona a propósito: Blobs serializa a JSON, así que quien lee nunca recibe una referencia viva, mientras que `coordinatorStore.js` (fase 09) sí devuelve referencias y muta en el lugar — un fake que no clonara dejaría pasar en verde código que contra Blobs pierde escrituras en silencio |
| D46 | 2026-08-06 | Etapa B — El token del paciente viaja en el encabezado `X-Visit-Token`, nunca en la query string, y `GET /api/visit` devuelve UNA sola respuesta 404 idéntica para token mal formado, encabezado ausente, visita inexistente y visita vencida. La caducidad (R1) se aplica dentro de `getVisitByToken`, no en el handler | Aceptar `?t=<token>` como en el enlace del QR; devolver 400 para el token mal formado y 404 para el inexistente; aplicar R1 en la Function | La URL completa queda en los registros de acceso de la plataforma, en el historial del navegador y en el `Referer`; el enlace del QR sí lleva `?p=` porque no hay otra forma de que un QR entregue la credencial (D01/D02), pero la petición que hace la app después no tiene por qué repetir esa exposición. Y un 400 distinto del 404 le regala a quien prueba tokens a ciegas un oráculo de cuáles vale la pena seguir intentando. Aplicar R1 dentro del store hace INV-3 estructural en vez de recordado: "vencida" e "inexistente" salen por la misma línea de código y no pueden divergir después. Los tres puntos están fijados por prueba, comparando `{status, body, headers}` completos |
| D47 | 2026-08-06 | Etapa B — Se publica UNA sola Function y es de solo lectura (`GET /api/visit`). El camino de escritura existe y está probado en `src/server/visitStore.js`, pero no tiene endpoint hasta que exista la autenticación de la Etapa C | Publicar ya el `POST` que crea visitas para poder probar el flujo completo antes; dejar el store sin escribir hasta la Etapa C | Publicar un POST sin auth es publicar una API abierta donde cualquiera crea —y por tanto guarda— datos de salud, en un sitio que ya está en vivo. El costo de esperar es tener que probar la creación por pruebas en vez de por curl durante una etapa; el costo de no esperar no se puede deshacer. El núcleo sí se escribe completo ahora para que la Etapa C solo agregue la sesión, no la lógica |
| D48 | 2026-08-06 | Etapa B — CSP real en `_headers`, con `script-src` por hash SHA-256 (uno por cada script en línea publicado) y sin `'unsafe-inline'`; `style-src` sí lo lleva, y es la única directiva que lo lleva. `test/deploy/csp.test.js` recalcula los hashes desde los HTML en cada corrida y `build.py` imprime el suyo al terminar | `'unsafe-inline'` en `script-src` (lo más simple); `<meta http-equiv>` en cada HTML; hashear también el CSS; seguir sin CSP y corregir solo la frase del README | El README afirmaba "CSP estricta" desde la fase 08 y no existía ninguna: ni `_headers`, ni `netlify.toml`, ni `<meta>`. Con datos clínicos entrando por red el hueco deja de ser de documentación. El CSS se arma en tiempo de ejecución concatenando las constantes de cada pantalla (`app.js:51`), así que hashearlo obligaría a recalcular la política cada vez que alguien toca un color — la excepción se escribe y se acota con un test que verifica que no se extienda a ninguna otra directiva, en vez de repetir que la CSP es "estricta". `form-action 'none'` no es adorno: los seis `<form>` del proyecto los intercepta JS y ninguno declara `action`, así que un Enter suelto navegaría a la MISMA URL con los campos en la query string — y en la app del paciente esa URL es la que trae `?p=<token>`. Un hash desfasado no rompe la suite ni el server local (ninguno manda encabezados): rompe la página en producción, en blanco y sin aviso — por eso el guard |
| D49 | 2026-08-06 | Etapa B — `Referrer-Policy: no-referrer` en todo el sitio | Dejar el default del navegador (`strict-origin-when-cross-origin`); ponerlo solo en la app del paciente | Fuga real, no hipotética: el token del paciente viaja en la URL (`/?p=<token>`, D01/D02) y `screens/help.js:19` abre `https://wa.me/...`. Con el default del navegador, tocar "WhatsApp" desde la pantalla de ayuda le manda a whatsapp.com un `Referer` con la credencial completa de la visita dentro. `strict-origin-when-cross-origin` recorta la ruta solo cuando cambia el origen —que es este caso— pero basta un enlace mismo-origen para que la URL entera vuelva a viajar, así que el default no alcanza como garantía. `no-referrer` no cuesta nada aquí: la app no depende del `Referer` para nada |
| D50 | 2026-08-06 | Etapa B — Bug real encontrado y corregido: `index.html` estaba 7,862 caracteres atrás de `src/`. Se agrega `python3 build.py --check` (reconstruye en memoria, compara, no escribe) y `test/deploy/bundleUpToDate.test.js`, que lo corre dentro de `npm test` | Reconstruir y ya, confiando en acordarse la próxima vez; poner el build como `command` en `netlify.toml` para que Netlify reconstruya en cada deploy | La Etapa A cambió 18 archivos de `src/ui` y nadie corrió `build.py`: el commit quedó verde con 308/308 y el sitio publicado siguió sirviendo el bundle anterior, así que ninguno de esos 18 arreglos estaba en vivo —incluido el catálogo de rutas de D41, que es justo lo que el cliente pidió para que el mapa funcionara. Ninguna prueba podía notarlo: todas importan de `src/`, que estaba bien; el que estaba mal era el único archivo que ve el paciente. Con la CSP de D48 el costo sube de "versión atrasada" a "página en blanco". Mover el build a Netlify volvería irrelevante el `index.html` versionado y ataría el despliegue a que su imagen traiga python3 — se prefiere que el artefacto siga versionado y que algo grite cuando se desincroniza |

| D51 | 2026-08-06 | Etapa B — Bug real corregido en el propio guard de D48: el extractor de scripts en línea quita los comentarios HTML ANTES de buscar, y se agrega un test que verifica que lo extraído se parezca a JavaScript (`<script`, `-->` y `<title` no pueden aparecer dentro). La misma corrección va en `build.py` | Dejar el extractor como estaba (la suite lo daba por bueno); usar `<body`/`<div` como marcas de derrame | El encabezado de `coordinator.html` contiene el texto literal `<script type="module">` explicándose a sí mismo. La búsqueda enganchaba esa mención DENTRO del comentario y se llevaba de corrido el resto del comentario, el `<title>` y el `<body>` hasta el primer `</script>` real: un "script" de ~900 caracteres de prosa cuyo hash ningún navegador iba a calcular jamás. Resultado: `_headers` autorizaba un texto inexistente, la suite pasaba en verde con 383/383, y el panel de coordinación quedaba EN BLANCO en producción. No lo encontró ninguna prueba —el guard estaba de acuerdo consigo mismo— sino cargar los tres documentos en el navegador con la CSP puesta como `<meta http-equiv>`, donde Chrome dijo qué hash esperaba de verdad (`Zhp3hj…`) contra el que había (`Omz48D…`). `<body` y `<div` se descartaron como marcas de derrame porque el bundle de `index.html` los trae a montones dentro de plantillas de JS, que es justo lo que hace la app: la marca tiene que distinguir derrame de código legítimo, no solo verse estricta. La lección se queda escrita en el archivo: un guard que deriva el valor esperado con el mismo código que produce el valor real no verifica nada hasta que alguien lo contrasta contra el sistema de verdad |
| D52 | 2026-08-06 | Etapa C — PBKDF2-SHA256 con 600,000 iteraciones, y el número de iteraciones se guarda DENTRO del registro de cada contraseña (`pbkdf2-sha256$600000$sal$llave`) | Argon2id (lo que recomienda OWASP de primera); scrypt; bcrypt; fijar las iteraciones en una constante global | Argon2id necesita una dependencia nativa que compilar, y este proyecto corre en el runtime de Netlify sin control sobre la imagen. scrypt sí viene en `node:crypto`, pero pide memoria por cada intento y la memoria es justo el recurso escaso en una función serverless — con un endpoint de login abierto, cuánta se pide lo decide quien ataca. PBKDF2 es el respaldo que OWASP nombra para exactamente este caso y no sale del runtime que ya hay. Guardar las iteraciones en el registro es lo que permite subir el costo después sin dejar fuera a las cuentas viejas: sin eso, cambiar la constante invalida todas las contraseñas ya guardadas. El verificador acota el valor leído (1..10,000,000) para que un registro manipulado no se convierta en un colgón de CPU |
| D53 | 2026-08-06 | Etapa C — La sesión es una cookie firmada (no cifrada) con HMAC-SHA256, `HttpOnly; Secure; SameSite=Strict`, 8 horas; el servidor no guarda sesiones. `requireCoordinator()` vuelve a leer la cuenta en el almacén en cada mutación | Sesiones con estado en Blobs; JWT con una librería; cifrar el contenido de la cookie; `SameSite=Lax`; confiar solo en la firma sin releer la cuenta | Sin estado evita una lectura por petición y una tabla de sesiones que limpiar, pero tiene un defecto que no se puede tapar con criptografía: una cookie firmada no se puede revocar antes de que caduque. Releer la cuenta lo compra barato —una lectura en rutas que de todos modos ya escribían— y hace que dar de baja a alguien surta efecto en la siguiente petición en vez de hasta 8 horas después. La cookie va firmada y NO cifrada a propósito, y hay una prueba que lo fija: así nadie mete un secreto de servidor ahí dentro creyendo que está oculto; lo que lleva es usuario, nombre visible y caducidad, todo cosas que esa persona ya sabe. `SameSite=Strict` es TODA la defensa CSRF del panel, y alcanza porque `coordinator.html` es estático y pide la sesión después desde el mismo origen. `SESSION_SECRET` no tiene valor por defecto: si falta, la Function truena con 500 desde el primer intento. Un respaldo silencioso significaría que la misma llave firma en todas partes y cualquiera que haya visto el repo se fabrica una sesión |
| D54 | 2026-08-06 | Etapa C — Las tres formas de fallar el login (usuario inexistente, contraseña mala, cuenta bloqueada) devuelven la MISMA respuesta 401, byte por byte; con usuario inexistente igual se paga una derivación PBKDF2 completa. Bloqueo de 15 minutos tras 5 intentos, que se vence solo | Distinguir "usuario no existe" de "contraseña incorrecta"; avisar que la cuenta está bloqueada; saltarse el hasheo cuando el usuario no existe (lo obvio); bloqueo permanente hasta que un administrador lo levante | Un mensaje distinto por caso convierte el login en un directorio de quién trabaja en coordinación, y saltarse el hasheo hace lo mismo con un cronómetro: la respuesta rápida delata que la cuenta no existe. Se prueba con un espía sobre la función de derivación, no midiendo tiempos, porque una prueba de reloj es inestable y falla sola en CI. La igualdad de las tres respuestas se fija comparando estado, cuerpo y `Set-Cookie` completos, así que agregar un detalle "útil" a una de ellas rompe la suite. El bloqueo se vence solo porque si no, cualquiera que sepa un nombre de usuario deja fuera a esa coordinadora cuando quiera, con cinco intentos basura; un bloqueo permanente cambia un ataque de adivinanza por uno de negación de servicio contra la persona. Limitación conocida y anotada en el código: Blobs no tiene comparar-y-fijar, así que con peticiones simultáneas el contador puede quedarse corto — frena el intento a fuerza bruta, no lo vuelve imposible |
| D55 | 2026-08-06 | Etapa C — El manejo de teclas del lector de contraseña de `scripts/create-coordinator.mjs` se extrae a una función pura (`applyKey`) con 9 pruebas, y las teclas de control se construyen con `String.fromCharCode(0x7f)` en vez de escribirse como literal | Dejar la lógica dentro del manejador de la terminal y probarla a mano; escribirlas como carácter crudo; escribirlas como escape `\u007f` | El lector corre sin eco y en modo crudo: no se ve al usarlo ni al revisarlo. Y las constantes que maneja son invisibles en el fuente — U+007F crudo se ve idéntico a una cadena vacía en el editor, y `JSON.stringify` tampoco lo escapa, así que ni inspeccionarlo a ojo lo distingue. Si DEL se perdiera al guardar el archivo, la rama de borrado dejaría de activarse y, como DEL pasa el filtro de imprimibles, la tecla de borrar AGREGARÍA un carácter a la contraseña en vez de quitarlo; nadie lo notaría hasta que alguien no pudiera entrar. `fromCharCode` es ASCII puro y ninguna herramienta lo puede estropear. Una prueba encontró además que las flechas inyectaban `[A` en la contraseña, porque la secuencia de escape se colaba carácter por carácter. Nota de honestidad: durante esta etapa reporté esa rama como rota en el archivo real; era una lectura equivocada de la salida de `JSON.stringify`, el carácter sí estaba. El riesgo es real y la extracción se queda, pero el bug concreto que dije haber encontrado no existía |
| D56 | 2026-08-07 | Etapa D — `coordinatorStore.js` deja de ser una copia en memoria de las fixtures y habla con la API. Guarda DOS mapas separados (el resumen de la lista y el expediente completo de cada visita) y expone tres estados de espera —`checking`, `loading`, `error`— que el panel pinta como puerta (`renderCoordinatorGate`), en vez de pintar la pantalla vacía. La validación de `addAppointment`/`editAppointment` vive en el servidor y el formulario la refleja; los códigos de razón (`required`, `unknown`, `invalidDate`, `noOffset`, `order`, `tooLong`) se traducen en la pantalla | Un solo mapa con lo que haya llegado; pintar la pantalla vacía mientras la respuesta viaja; seguir validando solo en el formulario ahora que hay backend; dejar que el servidor devuelva el texto del error ya traducido | Un solo mapa obliga a que el resumen de la lista y el expediente completo compartan forma, y entonces `visit.appointments === undefined` significa a la vez "esta visita no tiene citas" y "esta visita nunca se ha abierto": el panel no puede distinguirlas y acaba diciendo la primera cuando la cierta es la segunda. Lo mismo con la pantalla vacía durante la carga — es una afirmación falsa sobre el expediente de un paciente, no un detalle estético, y es justo el momento en que una coordinadora podría capturar dos veces la misma cita. La validación en el servidor no es redundancia con el formulario: el formulario lo escribe el mismo bundle que cualquiera puede editar en su navegador, así que hasta la Etapa D el endpoint aceptaba una cita con fecha inventada y sin ubicación. Y los códigos viajan sin traducir porque el idioma lo elige quien mira la pantalla, no quien guarda el dato: un texto ya traducido por el servidor obliga a que el servidor sepa el idioma del panel y deja el mensaje fuera de `i18n.js`, donde vive todo lo demás |
| D57 | 2026-08-07 | Etapa D — Las pruebas de las pantallas del panel corren contra el handler REAL del servidor (`src/server/coordinatorHandler.js`) sobre un KV en memoria, vía `test/helpers/loopback.js`, en vez de contra un doble escrito a mano. Se reescriben `visits`, `itinerary`, `lodging` y `qpass` para crear sus datos por la API y volver a leerlos con un SEGUNDO store | Un doble del `api` que devuelva `{status, body}` a mano por cada caso; mantener las fixtures `v_demo1`/`v_demo2` como base de las pruebas de pantalla | Un doble escrito a mano es una copia de las reglas del servidor, y una copia se desincroniza en silencio: el día que el handler cambie un código de razón o el nombre de un campo, las pruebas de pantalla siguen verdes contra la versión vieja. Con el loopback, ese cambio rompe las pruebas de pantalla en la misma corrida. El segundo store no es adorno: es literalmente lo que pasa cuando la coordinadora recarga el panel o lo abre en otra máquina, así que la prueba de "recargar ya no borra nada" (#16) es la afirmación real y no una simulación de ella. Lo que el loopback finge es solo el guardia de sesión, porque probar el login otra vez aquí sería repetir `test/server/authHandler.test.js` en cada pantalla |
| D58 | 2026-08-07 | Etapa D — Las dos bases de la API viven como constantes exportadas (`COORDINATOR_BASE`, `AUTH_BASE`) y el cliente de acceso se arma con su propia función, `createAuthApi()`, no con `createHttpApi({ base })` en el llamador. `test/ui/api.test.js` las ata a `LOGIN_PATH`/`LOGOUT_PATH`/`SESSION_PATH`/`COORDINATOR_PREFIX`, que exporta el servidor. Se agrega también verificación en navegador como parte de la etapa, no como opcional | Un solo transporte para todo (lo que estaba); pasar la base en el llamador; dar la etapa por terminada con la suite en verde | Bug real, encontrado solo en navegador: `boot()` le pasaba el MISMO `api` —base `/api/coordinator`— al store y al cliente de acceso, así que entrar pegaba en `/api/coordinator/login`, que no existe. La pantalla de acceso se veía perfecta y nadie podía entrar. Los 636 casos en verde no lo veían porque `authClient.test.js` inyecta un doble que solo observa `'/login'`, sin base: la ruta relativa era correcta y el destino no. Pasar la base en el llamador deja el mismo hueco abierto —depende de que quien arma el cliente se acuerde—, y por eso la función propia. Atar el test a las constantes del servidor evita la otra versión del mismo error: que el cliente y el servidor coincidan con cadenas escritas dos veces hasta que alguien cambie una. La lección general se repite de D51: una suite que solo se compara consigo misma no prueba que el sistema funcione, y un control muerto más —"Volver a visitas" heredado por la pantalla de acceso, el #20 otra vez— también salió de abrir el panel, no de correr las pruebas |
| D59 | 2026-08-07 | `netlify/functions/_kv.mjs` pide `consistency: 'strong'` a Netlify Blobs, y si el runtime no expone `uncachedEdgeURL` la Function truena con 500 en vez de degradar a lectura eventual. Se agregan dos verificaciones nuevas: `test/deploy/blobsKv.test.js`, que corre el adaptador contra un `BlobsServer` de verdad (viene dentro de `@netlify/blobs`, sin red ni credenciales), y `scripts/smoke-blobs.mjs`, que crea y borra una visita marcada `PRUEBA-SMOKE` en el sitio desplegado | Dejar la consistencia por defecto (lo que estaba); poner un respaldo silencioso a eventual cuando falte `uncachedEdgeURL`; seguir sin probar el adaptador porque D45 lo dejó fuera a propósito; probar la consistencia comparando el texto del archivo con una expresión regular | Bug real en código ya empujado, encontrado leyendo el cliente de `@netlify/blobs` para contestar "¿cómo pruebo contra Blobs de verdad?", no corriendo nada: el cliente arranca en `eventual` si no se le dice nada, y dentro de una Function eso manda las lecturas al edge CON caché. Todo el servidor de este proyecto es leer-modificar-escribir, así que dos cambios seguidos sobre la misma visita pierden el primero en silencio — se agrega la cita A, se escribe; se agrega la B partiendo de una lectura anterior a A y se guarda la visita con B y sin A. Ningún error, ningún aviso, sobre el expediente de un paciente. El respaldo silencioso se rechaza por lo mismo que `SESSION_SECRET` no tiene valor por defecto (D53): reintroduce el bug exacto que la línea evita y encima lo esconde; un 500 se ve el primer día, una cita que desaparece no. La regla que deja este hallazgo: D45 mantuvo la lógica pura y probable dejando FUERA las veinte líneas del adaptador, y ahí es donde estaba el bug — un pedazo sin probar no es un pedazo sin riesgo, es donde se acumula. El test de consistencia es de comportamiento y no de texto: una expresión regular sobre el fuente solo demuestra que una cadena está escrita, así que se usa la maquinaria del propio cliente (una lectura fuerte sin `uncachedEdgeURL` tira `BlobsConsistencyError`) más un test de contraste que prueba que la afirmación no es vacía. Lo que aún no se puede probar local es el comportamiento de producción con DOS direcciones de edge, y para eso es el smoke: exige `NETLIFY_SITE_ID`/`NETLIFY_AUTH_TOKEN` y se niega a empezar sin ellas, porque la API no tiene DELETE de visitas y sin credenciales dejaría un expediente de mentira suelto en producción |
| D60 | 2026-08-07 | Etapa E — `src/render/qr.js` deja de ser solo versión 3: se parametriza por versión con una tabla (`QR_VERSIONS`), se implementa el entrelazado de dos bloques que la versión 4 exige, y `generateQrMatrix` elige la versión MÁS CHICA donde quepa el payload en vez de mudar todo a la 4. La comparación módulo a módulo contra nayuki/QR-Code-generator deja de ser un ejercicio de una sola vez y queda congelada en `test/render/qrReference.fixture.js`, regenerable con `scripts/gen-qr-reference.py`. Se elimina el export `QR_CONFIG` | Mudar todos los símbolos a la versión 4; un alias corto solo para el QR (el plan B del plan); dejar `QR_CONFIG` como estaba apuntando a la versión 3; escribir el fixture a mano; dejar el generador del fixture en el scratchpad como la primera vez | El enlace de visita con token de 128 bits mide 55 bytes y en la versión 3 caben 42, así que la 4 era obligatoria. Pero el QPASS de la fase 06 sigue pendiente de la prueba con lectora física que el README lista como del cliente: subirlo a la 4 en silencio cambiaría justo el símbolo que están por probar e invalidaría esa verificación, y por eso lo que cabía en la 3 sale idéntico módulo por módulo. `QR_CONFIG` se borra en vez de conservarse porque su `maxBytes` valía 42 mientras el generador ya acepta 62: un consumidor que lo leyera rechazaría payloads que sí caben, y un límite obsoleto es peor que ninguno. El entrelazado es la razón de fondo del fixture: un error ahí produce un símbolo que se ve perfecto y que `decodeQrMatrix` lee sin quejarse —porque de-entrelaza con el mismo error— pero que ningún teléfono puede leer; es la misma trampa que la fase 06 documentó y la única defensa es una segunda implementación. El generador del fixture entra al repo porque la primera versión se escribió en el scratchpad, que se borra: un archivo generado que nadie puede volver a generar se vuelve intocable y la comparación deja de ser verificación. Ese script además fuerza modo byte — `QrSegment.make_segments()` elige el modo óptimo y para `"A"*42` eligió ALFANUMÉRICO, otra codificación; el caso falló hasta que se detectó, y es exactamente el tipo de diferencia que una referencia sirve para encontrar. Verificación externa aparte, como en la fase 06: los 10 símbolos que emite este archivo se decodificaron con `QRCodeDetector` de OpenCV —ajeno a nayuki y a mi propio decodificador— y los 10 coincidieron |
| D61 | 2026-08-07 | Etapa E — La entrega al paciente es una PESTAÑA de la visita (`screens/coordinator/handoff.js`: enlace, QR y botón de WhatsApp), no un aviso que aparezca al crearla, y el alta sigue aterrizando en `#/itinerary`. El enlace es `https://<sitio>/v/<token>`, traducido por una regla `/v/*` de `_redirects` a `?p=<token>` con **302 y no 200**. `wa.me` va SIN número de teléfono, y el mensaje se arma en el idioma del PACIENTE, no en el del panel | Un aviso con el QR después de crear la visita; aterrizar en la pestaña de entrega; usar `?p=<token>` como enlace directo; un rewrite (200); guardar el teléfono del paciente para armar `wa.me/<numero>`; reusar `.nc-copy-feedback` de `screens/stay.js` | Esta pantalla es la respuesta literal a la pregunta que abrió todo: hasta la Etapa D la palabra `token` no aparecía en ninguna de las cinco pantallas del panel, así que el QR no se generaba en ningún lado. Pestaña y no aviso porque un aviso muere con la primera recarga y el enlace es lo ÚNICO que existe para llegar al paciente: si se pierde, la visita queda muda. Se puede volver a ella mañana, que es el orden en que de verdad se trabaja — y por eso el alta sigue cayendo en el itinerario: mandarle a alguien un enlace a un itinerario vacío es peor que no mandarle nada. El 302 es la parte que solo falla en producción: un rewrite sirve la página pero deja la barra en `/v/<token>`, `params.get('p')` devuelve null y el paciente ve "este enlace no está disponible" con un enlace perfectamente válido en la mano — no falla en `python3 -m http.server`, ni en la suite, ni en el e2e, y por eso existe `test/deploy/redirects.test.js`. Sin número en `wa.me` porque el `Visit` no guarda teléfono (PRD §7) y agregarlo sería empezar a almacenar un dato personal que nadie pidió; sin número, WhatsApp abre con el texto listo y la coordinadora elige el chat, que además es donde confirma con los ojos a quién se lo manda. El idioma del mensaje es el del paciente porque la coordinadora puede tener el panel en español y estar atendiendo a alguien que solo lee inglés — es el único lugar del proyecto que traduce dos idiomas a la vez en la misma pantalla. Tres estados distintos de "no hay enlace" con tres mensajes honestos en vez de uno vago: visita inexistente, expediente sin token (`GET /visits` lo borra a propósito) y `origin` inservible — `location.origin` es la cadena literal `"null"` en `file://`, y un doble clic en `coordinator.html` habría armado `null/v/<token>`: un enlace roto que se ve igual de real que uno bueno y que sale por WhatsApp hacia un paciente. El QR degrada a solo-enlace si no cabe (hoy sobra: 55 bytes de 62) en vez de tumbar la pantalla el día del dominio propio más largo |
| D62 | 2026-08-07 | Etapa E — `src/ui/app.js` deja de resolver el token solo contra las fixtures del bundle: `boot()` pasa a ser async y la resolución se extrae a `src/ui/visitSource.js` con orden **fixture → red → caché**. Un **404 BORRA** la caché local; una falla de red no. `isExpired` se vuelve a evaluar siempre, venga de donde venga el expediente. La caché (`src/ui/visitCache.js`) se guarda por TOKEN, y la validación de forma vive en un módulo compartido, `src/ui/visitRecord.js` | Caché primero (offline-first) y red en segundo plano; servir la caché ante un 404; guardar por `visitId`; extender `passCache.js` para que guarde el expediente entero; dejar la resolución dentro de `app.js`; duplicar el predicado de forma en `api.js` y en `visitCache.js` | Esta era la mitad no dicha de la pregunta original: aunque el panel hubiera mostrado el QR, `app.js:84` buscaba el token entre las fixtures estáticas y una visita creada por la coordinadora NUNCA se habría podido abrir. Las fixtures van primero para que `/demo` y las cinco visitas de ejemplo sigan abriendo en modo avión —un token real es base64url de 22, nunca colisiona con `fixture-token-*`— y offline-first se rechaza porque la coordinadora mueve citas y revoca pases DESPUÉS de mandar el enlace: mostrar primero lo viejo y corregir después es enseñarle al paciente una hora de cita que ya no es. El 404 borrando la caché es la línea más importante del cambio: el servidor contesta un único 404 para "no existe", "malformado" y "ya venció" (INV-3), así que servir la caché ahí sería deshacer la caducidad desde el cliente y una visita vencida seguiría abriéndose en ese teléfono para siempre; no basta con ignorarla esta vez, porque la siguiente apertura sin señal la resucita. Una falla de red, en cambio, es justo para lo que la caché existe (el PRD exige que el pase siga en pantalla sin señal en el acceso) y por eso NO la borra: perder el wifi no puede ser lo que tire el respaldo. `isExpired` se recalcula con el `now` de AHORA por el mismo criterio que `passCache` con R3 (INV-4): un expediente guardado ayer puede haber vencido durante la noche. Por token y no por `visitId` porque cuando hay que decidir si existe caché todavía no se conoce el id — viene dentro del expediente que se está intentando traer. Dos cachés y no una porque resuelven cosas distintas: `passCache` mantiene vivo el símbolo del pase con su marca de "guardado a las…", esta mantiene viva la APERTURA de la app; el costo es que los pases quedan guardados dos veces en el mismo origen. La resolución sale de `app.js` para poder probarse: `app.js` necesita DOM y este proyecto no monta fake DOM (D8). El predicado de forma se comparte porque escribirlo dos veces es exactamente lo que se arregla en un lado y se olvida en el otro — y además `build.py` aplana todo a un solo scope y lo habría rechazado como colisión de nombre, que es como se encontró |
| D63 | 2026-08-07 | Etapa F — `build.py` pasa de un objetivo a dos, con una tupla `TARGETS`. El panel se publicaba **sin construir**: `coordinator.html` era el fuente con `<script type="module">`, así que cada visita disparaba ~30 peticiones encadenadas, obligaba a publicar todo `src/` y —lo grave— su hash de CSP solo cubría el stub de dos líneas mientras el código real entraba bajo `'self'` sin hash. El fuente se renombra a `coordinator-app.html` (simétrico con `app.html`) y la salida generada se queda con el nombre `coordinator.html`, para **no mover la URL publicada**. | Dejar el panel sin construir (es interno, no lo usa el paciente); o publicar el bundle como `coordinator-built.html` y cambiar la URL. | Lo interno no es lo mismo que lo no publicado: el panel está en el mismo sitio abierto y lo usa gente bajo presión, en un lobby, con el wifi del hospital — 30 peticiones encadenadas se sienten ahí, no en la oficina. Y un hash que solo cubre el stub es peor que no tener hash, porque el guard de `csp.test.js` pasa en verde. Cambiar la URL habría roto el marcador de cada coordinadora por una razón puramente interna. |
| D64 | 2026-08-07 | Etapa F — `intake.js`, `itinerary.js` y `lodging.js` tenían cada uno un `const CAMPOS` privado de su módulo; se renombran a `CAMPOS_INTAKE`, `CAMPOS_CITA` y `CAMPOS_HOSPEDAJE`. | Dejarlos y hacer que `build.py` renombre automáticamente los símbolos que chocan al aplanar. | No era un problema de estilo: `build.py` aplana todo el grafo a un solo scope, y al empaquetar el panel por primera vez (D63) los tres colisionaron y **el build falló**, que es exactamente lo que debe pasar. Un renombrado automático en el empaquetador significaría que el código publicado no se llama igual que el código fuente, y sería la primera vez en este proyecto que un stack trace de producción no se puede leer contra `src/`. El conflicto llevaba meses ahí, dormido solo porque nadie había construido esto. |
| D65 | 2026-08-07 | Etapa F — El barrido de imports muertos se vuelve prueba (`test/deploy/deadImports.test.js`), y cuenta como uso cualquier aparición del nombre, incluso en comentarios o cadenas. | Agregar ESLint, que lo haría mejor y de paso otras 200 cosas. | Encontró dos (`generateCode128Bars` en `screens/pass.js`, `classNames` en `components/badge.js`) y ninguno se iba a encontrar solo: el proyecto no tiene linter a propósito (D2, cero dependencias de desarrollo), así que el chequeo tiene que vivir donde sí se corre. Conservador a propósito: una prueba estática que grita en falso se termina borrando, y entonces no protege de nada. |
| D66 | 2026-08-07 | Etapa F — `engines.node` sube de `>=20` a `>=22`. | Dejarlo en `>=20` y no tocar nada, que es lo que llevaba meses. | Era falso: `npm test` corre `node --test 'test/**/*.test.js'`, y el glob como argumento posicional de `--test` es de Node 21. En Node 20 ese comando busca un archivo llamado literalmente `test/**/*.test.js` y falla. `>=22` en vez de `>=21` porque 21 no es LTS y no tiene sentido apuntar a la gente ahí. |
| D67 | 2026-08-07 | Etapa F — El directorio de publicación se queda en `publish = "."`, con el hueco documentado en el README en vez de arreglado. | Armar un `dist/` en `build.py` y publicar solo eso, que es lo correcto. | Publicar la raíz expone `src/`, `docs/` y `test/` en el sitio. No es información secreta —es el mismo repositorio público— pero tampoco hay razón para servirla. El arreglo se comprueba de una sola forma, desplegando, y desde aquí no se puede desplegar: cambiar el directorio de publicación a ciegas rompe un sitio que hoy está en línea y sirviendo, y ese riesgo es mayor que el de seguir exponiendo código que ya es público. Queda como primer punto de lo que siga. |
| D68 | 2026-08-07 | Etapa G — El traslado contratado es una entidad propia, `transfers[]`, hermana de `lodging` dentro del expediente. Ni un campo de `Lodging` ni un `Appointment` con tipo especial | Meterlo en `Lodging` (el hotel lo organiza); modelarlo como `Appointment` con `serviceName: "Traslado"`; una entidad global de traslados con `visitId` como referencia suelta | `Lodging` es una estancia con check-in y check-out, y un traslado es un evento con hora, punto de encuentro, chofer y vehículo: la mitad de sus campos no tendrían dónde vivir, y una visita puede tener traslado sin hotel (paciente que cruza por CBX y se regresa el mismo día). `Appointment` estaba más cerca y se rechazó por un motivo concreto y verificable: `locationId` apunta al catálogo de `src/data/locations.js`, que tiene exactamente 7 ids —ninguno es el aeropuerto— y ese catálogo alimenta el ruteo del mapa; forzar un traslado ahí obligaría a inventar una ubicación sin `mapPointId` y a que el motor de rutas la esquivara con un caso especial. Además `nextStep` (R2) y sus ejemplos trabajados del PRD §8 son sobre citas: colar traslados en el mismo arreglo los habría cambiado sin que nadie lo pidiera |
| D69 | 2026-08-07 | Etapa G — `computeExpiresAt` e `isExpired` reciben el **expediente completo** (`record`) en vez de argumentos posicionales: `isExpired(record, now)` sustituye a `isExpired(visit, appointments, lodging, now)`. Se migran los 16 puntos de llamada y los 10 casos de `test/domain/expiry.test.js` | Agregar `transfers` como cuarto posicional antes de `now`, que era lo que pedía el spec y es el cambio de una línea | Con un cuarto posicional, un punto de llamada olvidado le pasa `now` al parámetro `transfers` y deja `now` en `undefined`; `instantMs(undefined)` es `NaN`, y `NaN > x` es `false`: **el enlace deja de caducar, en silencio**, que es exactamente lo que R1 existe para impedir y el peor fallo posible en una app con datos de salud. Con el expediente, ese mismo olvido revienta al leer `.visit` de `undefined` — ruidoso, en la primera corrida, en vez de callado y en producción. El costo es real (16 puntos de llamada tocados a mano y 10 casos de prueba reescritos) y se paga una sola vez |
| D70 | 2026-08-07 | Etapa G — Los puntos de encuentro viven en `src/data/transferPoints.js`, catálogo **separado** de `locations.js`, con su propia forma (`id`, `name: {es,en}`, `unconfirmed`) y sin `mapPointId`. Arranca con `tij_terminal`, `cbx` y `san_ysidro` marcados `unconfirmed: true`; `quartz` y `lobby_torre` confirmados | Agregarlos a `locations.js` con `mapPointId: null`; usar texto libre para el punto de encuentro | Las 7 ubicaciones de `locations.js` tienen `mapPointId` y participan en el ruteo: `resolveRoute` y el resaltado del mapa asumen que todo lo que está ahí se puede señalar en el plano, y un aeropuerto a 20 km no se puede. Un `mapPointId: null` habría hecho que cada consumidor del catálogo tuviera que acordarse de filtrar, y el que se olvide pinta un mapa roto. Texto libre se rechaza por D40 (todo lo enumerable es un `<select>`): "TIJ", "Aeropuerto", "aeropuerto de tijuana" y "Terminal 1" son el mismo lugar escrito de cuatro formas, y el paciente lee la que le tocó. `unconfirmed` es la misma convención que ya usan la farmacia y los horarios: se corrige editando un archivo, sin tocar código |
| D71 | 2026-08-07 | Etapa G — En Inicio el traslado va en **tarjeta propia, arriba** de "Tu siguiente paso". `nextStep()` (R2) no se toca: sigue siendo solo de citas. Su hermano para traslados es `nextTransfer()`, en el dominio y no en la pantalla | Meter el traslado en `nextStep()` para que "tu siguiente paso" fuera de verdad lo siguiente; ponerlo abajo, como un acceso rápido más; calcular el próximo traslado dentro de `screens/home.js` | Meterlo en `nextStep()` es lo más honesto de leer y lo peor de sostener: R2 tiene ejemplos trabajados en el PRD §8, pruebas que los reproducen dato por dato, y alimenta el botón que lleva al QPASS —"tu siguiente paso" abre el pase que abre la puerta, y un traslado no tiene pase—. Arriba y no abajo porque el día del paciente empieza con el coche: a las 06:00 lo que importa es dónde lo recogen, no la cita de las 08:00. `nextTransfer` vive en el dominio por lo mismo que `nextStep`: "cuál es el siguiente" es una regla, y una regla dentro de una pantalla no se puede probar sin DOM (D8). Sin traslado por venir no se pinta nada y la pantalla queda idéntica a como estaba |
| D72 | 2026-08-07 | Etapa G — El nombre y el teléfono del chofer se guardan en el expediente y se sirven a cualquiera con el token. Queda **señalado** en el PRD §15 punto 9 como decisión del hospital, no resuelto aquí | Cifrar o esconder el teléfono detrás de un segundo paso; no guardarlo y dejar que el paciente llame a coordinación; guardarlo y no decir nada | Contrasta a propósito con D61, donde se evitó guardar el teléfono del PACIENTE: aquí el dato es de un tercero, un proveedor identificado y contratado por el hospital, y el paciente lo necesita en el peor momento posible —parado en la banqueta del aeropuerto, con el coche sin aparecer y sin señal para llamar a nadie más—. Esconderlo detrás de un paso más lo vuelve inútil justo entonces. No guardarlo traslada el problema a coordinación, que puede estar cerrada a las 06:00. Y guardarlo sin decir nada es lo único inaceptable: quien firma el tratamiento de datos tiene que saber que este dato está ahí. La decisión de fondo (LFPDPPP, y lo que aplique del lado de EE.UU. por los pacientes que cruzan) ya estaba abierta en `docs/`; esto le suma un punto concreto en vez de esconderse debajo |
| D73 | 2026-08-07 | Etapa G — El teléfono del chofer es obligatorio en **E.164 con `+`** (`/^\+[\d\s-]+$/`, entre 8 y 15 dígitos) cuando se captura, y el chofer y el vehículo enteros son **opcionales**. La validación vive en el servidor (`validateTransferInput`) y el formulario la refleja | Aceptar el teléfono en formato local ("664 123 4567"), que es como lo dicta la gente; exigir chofer y vehículo al capturar el traslado | La pantalla del paciente arma `wa.me/<solo dígitos>`: sin lada de país, "6641234567" se interpreta como un número de otro país y el mensaje se le va a un desconocido — un fallo silencioso, del lado del paciente, que nadie en coordinación vería nunca. El `+` es la única forma de que `tel:` y `wa.me` signifiquen lo mismo desde un teléfono mexicano y desde uno estadounidense, que es exactamente la mezcla de pacientes de este hospital. Chofer y vehículo opcionales porque el traslado se contrata días antes y al chofer lo asignan la víspera: exigirlos obligaría a inventar datos o a no capturar el traslado, y ambas cosas son peores que un campo vacío. Mismo precedente que `reservationCode` en el hospedaje. Solo `kind`, `scheduledAt` y `meetingPointId` son obligatorios — qué, cuándo y dónde |
| D74 | 2026-08-07 | Etapa G — La tarjeta de traslado de Inicio antepone el día (`formatDayLabel`) cuando el traslado **no** es hoy, y solo la hora cuando sí lo es | Dejar solo la hora, como la tarjeta de R2; poner siempre el día completo | Se descubrió verificando en el navegador, no en las pruebas: la tarjeta decía "3:00 PM · Quartz Hotel & Spa" debajo de un encabezado que dice "Hoy · Miércoles 11", y el traslado era del día siguiente. El regreso cae por definición el último día de la visita, así que durante casi toda la estancia esta tarjeta anuncia algo que no es hoy — el caso ambiguo no es la excepción, es lo normal. Copiar a R2 tal cual no servía: `nextStep()` también puede devolver una cita de mañana, pero esa tarjeta lleva al QPASS y el paciente la contrasta con el itinerario; aquí no hay nada con qué contrastar y el error se paga perdiendo el vuelo. Poner siempre el día repetiría "Hoy · Martes 10" dos veces en la misma pantalla, así que la condición se ata al encabezado: si el rótulo del traslado es idéntico al de la pantalla, sobra. R2 no se toca (D71 sigue en pie) |
| D75 | 2026-08-07 | Etapa H — Las fechas del panel se capturan con el **control nativo** del navegador: `<input type="date">` para la ventana de la visita, `<input type="datetime-local">` para citas, hospedaje y traslados. Se acabaron los `<input type="text">` con un ISO de ejemplo en el `placeholder`. Extensión natural de D40, y con el mismo límite: el control es **comodidad, no autoridad** — el servidor sigue validando exactamente lo mismo (`checkFecha`: `required`/`invalidDate`/`noOffset`) y sigue contestando 422 | Texto con máscara de entrada (un `input` que se va formateando al teclear); tres `<select>` de día/mes/año; dejarlo como estaba porque "la coordinadora ya se acostumbró" | El texto libre pedía teclear `2026-03-10T08:00-07:00` de memoria, incluido el desplazamiento horario — un formato que nadie escribe bien dos veces seguidas y que además cambia dos veces al año. Una máscara es código propio que hay que mantener y que sigue sin dar calendario ni validar el 31 de febrero. Tres `<select>` son doce clics para una fecha y no representan la hora. El control nativo ya trae calendario, teclado numérico en el teléfono, formato según el idioma del navegador y rechazo de fechas imposibles, sin una línea de librería. El `placeholder` con ISO de ejemplo se quita en el mismo movimiento: era la muleta del campo de texto, y encima de un control nativo solo estorba |
| D76 | 2026-08-07 | Etapa H — El desplazamiento de Tijuana se **deriva de la fecha** con `Intl` (`tijuanaOffset`), nunca se escribe fijo. Los helpers `tijuanaOffset`/`toIsoTijuana`/`toLocalInput`/`toDateInput` viven en `src/domain/time.js` y reutilizan el `tijuanaOffsetMinutes` que ya usaba `toTijuanaIso`. Sus pruebas viven en `test/domain/` para heredar `npm run test:tz` | Escribir `-07:00` fijo, que es el que traen todas las fixtures; usar `timeZoneName: 'shortOffset'` como segunda fuente de verdad; cortar los primeros 16 caracteres del ISO para prellenar | Un control nativo devuelve `2026-03-11T15:00` pelado, sin zona, y lo que se guarda es ISO con desplazamiento (PRD §7). Escribir `-07:00` fijo acierta de marzo a noviembre y **corre todas las citas una hora el resto del año**: Baja California no abolió el horario de verano junto con el resto de México, sigue el calendario de EE. UU. por la frontera. `shortOffset` sería una segunda fuente de verdad que hay que parsear de un string ("GMT-7") y que se puede desincronizar de la que ya existe y ya está probada. Y el corte de 16 caracteres funciona solo mientras **todo** venga en offset de Tijuana: el día que entre un `...Z`, devuelve la hora UTC como si fuera local y la corre siete horas **sin decir nada** — por eso `toLocalInput` calcula el instante real y exige que el reloj de pared de Tijuana en ese instante sea exactamente lo que dice el ISO, y devuelve `''` (campo vacío, visible) cuando no cuadra. Todo es función de su argumento: INV-1 e INV-5 intactos |
| D77 | 2026-08-07 | Etapa H — `checkIn`/`checkOut` del hospedaje se capturan con `datetime-local` (**con hora**), no con `date` como pedía el encargo. Las fechas de la visita sí son `date` | Seguir el encargo al pie de la letra y capturar solo el día; capturar el día e inventar una hora de hotel fija (15:00/12:00) al guardar | Aquí la hora no es adorno: `src/ui/screens/stay.js` le pinta al paciente "Martes 10 · 3:00 p.m." leyendo estos mismos campos, y `computeExpiresAt` (R1) usa `checkOut` para decidir hasta cuándo vive su enlace. Capturar solo el día dejaría dos opciones, las dos malas: pintarle "12:00 a.m." al paciente —una hora que nadie le dijo y que no es la del hotel—, o inventar 15:00/12:00 por defecto, que es un dato falso metido por la aplicación y que además se le muestra al paciente como si lo hubiera confirmado el hotel. La ventana de la visita sí es de días de verdad: `visit.startsAt/endsAt` no se le muestran al paciente en ningún lado, solo acotan el panel |
| D78 | 2026-08-07 | La extracción de scripts para el hash de la CSP (build.py y csp.test.js) recorre el documento saltando comentarios solo FUERA de `<script>`; dentro, los bytes van crudos. Un test nuevo exige que lo extraído aparezca literal en el archivo. | Quitar los comentarios HTML antes de buscar (lo que había), o volver a la búsqueda directa sin filtro. | Cada opción rechazada rompió producción de verdad: la búsqueda directa enganchó un `<script>` mencionado en un comentario (Etapa F), y el filtro de comentarios borró un `<!-- -->` legítimo de 393 bytes dentro de una plantilla JS del bundle del panel — el guard hasheaba 254220 bytes, el navegador 254613, y el panel quedó EN BLANCO al desplegar, con la suite en verde. El navegador hashea los bytes literales; cualquier normalización previa produce un hash que nadie va a calcular. |
